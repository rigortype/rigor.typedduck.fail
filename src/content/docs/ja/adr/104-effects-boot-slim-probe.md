---
title: "ADR-104 — エフェクトサーフェスのためのブートスリムプローブ"
description: "rigortype/rigor docs/adr/104-effects-boot-slim-probe.mdの翻訳です。"
editUrl: "https://github.com/rigortype/rigor/edit/master/docs/adr/104-effects-boot-slim-probe.md"
sourcePath: "docs/adr/104-effects-boot-slim-probe.md"
sourceSha: "8fb6180a35af8b4e8a068e9898a943721bc4495117a1c1ae7ab02aea1d72e0de"
sourceCommit: "74330669d672d9291ca22525402f61fbcbb09ceb"
translationStatus: "translated"
sidebar:
  order: 4104
---

ステータス: **Proposed、2026-08-25**。実装はまだない。2026-08-25の機能ウォーム/コールドキャンペーン（[`docs/notes/20260825-feature-warm-cold-corpus-perf.md`](../../notes/20260825-feature-warm-cold-corpus-perf/)）に基づく。

## コンテキスト

2026-08-25のキャンペーンがエフェクトテーブルのサイドカー（#475）、合成スキャンの短絡（#477）、そしてエンベロープ位置の遅延化（#479）を着地させたあと、ウォームな`rigor effects` / `rigor effects check`はほとんど何の作業もしていない——それでいてウォームな`rigor check`の2〜2.5倍かかる。mastodonでの計測では、checkの0.37秒に対して0.74秒であり、そのうち実行中の作業は0.35秒で、残りはプロセスのブートだ——推論エンジンと環境層の`require`であり、2つのキャッシュキーを計算して2つのキャッシュ値を採用するためだけにロードされている。`rigor check`はすでにこれを払うことを拒んでいる: ADR-87のWD4の`RunCacheProbe`は、`rigor/inference`を`$LOADED_FEATURES`へ入れることなくウォームなcheckを提供する。エフェクトのサーフェスには等価物がないので、あらゆるエフェクトのCIゲートが、Marshalのblobを読むために実行ごとのエンジンのブートを払っている。

実現可能性は、仮定ではなく現行のコードに対して検証した:

- 診断キーの半分はすでにエンジン不要である——`RunCacheProbe`が今日それを計算している（`lib/rigor/analysis/run_cache_key.rb`）。
- その上に載るエフェクトの同一性（`lib/rigor/effects/identity.rb`）は、語彙バージョン、カタログファイル、`effects:`ブロック、そして`PluginFacts#digest`をダイジェストする——そして`compute_digest`（`lib/rigor/effects/plugin_facts.rb:325`）は、ロードされたプラグインが宣言した寄与の純粋関数である。それが必要とするのは`Plugin::Loader`だけで、エンジンもプロジェクトの発見も決して必要としない。
- レポートは伝播されたテーブルとユニットごとのソースを必要とする;どちらも#475のサイドカーの中にある（`[collections, table]`——ソースはコレクションのサマリーキーから導出される）。祖先関係に依存する宣言レーンのリンク付け（#464/#467）は収集時に起こり、キャッシュされたサマリーへ焼き込まれるので、提供された実行がそれを失うことはありえない。
- `effects check` / `diff`はさらにレジストリ（プラグインのロード）、リーチのglob（設定）、そしてコミットされたスナップショット（YAMLの読み取り）を必要とする。`Snapshot.build`は0.006秒と計測された。

## 決定（提案）

checkのプローブと並べてエフェクトのプローブを追加する: `rigor effects`（レポート）・`effects check`・`effects diff`は、まず診断のディスクリプタとエフェクトのサイドカーから提供することを試み、設定・キャッシュ・プラグインローダー・`effects/*`の値層をロードする——`rigor/inference`はロードせず、`Environment`もロードしない。ミスした場合、および保存された値では答えられないモードの場合は、変更のないフルのRunnerのパスへ辞退する。

判定基準は、#442/#428の規則を一般化したものである: **プローブがサーフェスを提供してMAYよいのは、そのサーフェスが与えうるすべての答えが、保存された値と宣言だけから再現可能であるときに限る;生きた解析状態から計算される答えは、キャッシュエントリーに相乗りするか、さもなければ辞退を強制しなければならない**。 #428のファミリーは、この規則の2つの半分が別々に決められたときに何が起こるかを記録した;新しいプローブのサーフェスは、それを学び直す教訓としてではなく、設計上の前提条件として採用する。

実装時に決めることになる作業上の決定:

- **WD1**——辞退リスト: サイドカーまたは診断のミス、プールモードの設定、エディタのバッファ、そして答えが保存された値の純粋関数ではないあらゆるCLIのモード。判定時のスイッチ（`--no-tolerated-effects`）は提供可能なままである: `tolerated:`はエフェクトの同一性の中にあり、スイッチは提供されたテーブルがどう判定されるかを変えるだけだからだ。
- **WD2**——ADR-87の形での、空虚でない提供のスペック: 提供された側の腕は`rigor/inference`が`$LOADED_FEATURES`に不在であることをアサートし、コールド側の腕は同じ呼び出しがそもそもその答えを生成できることを証明する。
- **WD3**——プローブとRunnerは「コレクションからのソース」の実装1つとスナップショット構築の実装1つを共有するので、提供されたパスとフルのパスがドリフトすることはありえない。

## 却下／先送りした選択肢

- **デーモン／ウォッチモード**——すべてのコマンドについてブートのコストを平坦化する。計測された形を記録したうえで、すでにプロダクトの決定として先送りされている（2026-07-13のキャンペーンの§④）。このADRのプローブは、それを必要とすると計測された2つのサーフェスに対するCLI優先の答えである。
- **フルのパスのrequireを絞る**——エンジンの`require`*こそ*が大部分である;キーを計算するが解析はしない、というような`rigor/inference`の有用な部分集合は存在しない。
- **何もしない**——あらゆるエフェクトのゲートをcheckのウォームの床の2〜2.5倍のままに残す。その出費が遊んでいるブートであるときに、それは「エフェクトは高価だ」と読まれてしまう。

## 帰結

- ウォームな`rigor effects` / `effects check`は、checkと同等プラスプラグインのロードとMarshalの読み取り1回のところへ着地する（フェーズ帰属からの推定でmastodonでは約0.45秒;節約されるのはエンジンの`require`だ）。
- エフェクトのサーフェスが増えていく中で、2つ目のプローブのサーフェスは答えについて完全であり続けなければならない。上記の判定基準がそのガードであり、WD2の`$LOADED_FEATURES`のスペックが、黙った乖離を沈黙のレーンではなくレッドのスペックにする。**再評価のトリガー:**生きた解析状態から計算される新しいエフェクトの答えは、出荷される前にサイドカーのフィールドか辞退のどちらかを追加しなければならない。
- プラグインのロードは提供されるパスに残る（レジストリのラベルはそこから来る）——0.02〜0.04秒と計測されており、最適化の余地ではなく正しさのために必要なものだ。

## 他のADRとの関係

ADR-87のWD4（これが複製するcheckのプローブ）、ADR-45（実行結果キャッシュ）、ADR-103のWD12/WD13とissue #382/#475（これが読むサイドカー）、ADR-97（インデックス予算——このエントリーはルックアップのままである）。
