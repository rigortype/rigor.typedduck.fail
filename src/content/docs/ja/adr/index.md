---
title: "アーキテクチャ決定記録"
description: "rigortype/rigor docs/adr/README.mdの翻訳です。"
editUrl: "https://github.com/rigortype/rigor/edit/master/docs/adr/README.md"
sourcePath: "docs/adr/README.md"
sourceSha: "d142aa9553ebb3f12a60c12d39d6eb7ce65409977a4da1abfe3d8ff6b2b0693d"
sourceCommit: "eb8e9996d113a1b5e1778d0988597c979814a219"
translationStatus: "translated"
sidebar:
  order: 4000
---

このディレクトリにはRigorのアーキテクチャ決定記録（Architecture Decision Records、ADR）が含まれています。各ドキュメントは、重要な設計上の決定、その背景、検討された選択肢、そして結果を記録しています。

## 読み方

- **ADR-0**は基礎ドキュメントです — プロジェクトの中核的な原則とアーキテクチャを知るには、ここから始めてください。
- **ADR-1**から**ADR-3**は型モデル、拡張API、型表現を定義します — アナライザーの概念的な中核です。
- 番号の大きいADRは基礎の上に構築されており、必要に応じて読むことができます。
- 各ADRには**Status**フィールドがあります：`Accepted`、`Proposed`、`Superseded`のいずれかです。実装がまだ進行中のAcceptedなADRは、括弧書きの注記（例: *partially implemented*、*slice N deferred*）を持ちます。

## 索引

| # | Title | Status |
| --- | --- | --- |
| ADR-0 | [Rigorの基盤とコアアーキテクチャ](0-concept/) | Accepted |
| ADR-1 | [型モデルとRBSスーパーセット戦略](1-types/) | Accepted |
| ADR-2 | [拡張API戦略](2-extension-api/) | Accepted |
| ADR-3 | [内部型表現](3-type-representation/) | Accepted |
| ADR-4 | [型推論エンジンと`Scope#type_of`クエリ](4-type-inference-engine/) | Accepted |
| ADR-5 | [Rigor型のロバストネス原則](5-robustness-principle/) | Accepted |
| ADR-6 | [キャッシュ永続化バックエンド](6-cache-persistence-backend/) | Accepted |
| ADR-7 | [v0.1.0スライス4〜6作業上の決定](7-v0.1.0-slice-decisions/) | Accepted |
| ADR-8 | [Steepに着想を得た改善](8-steep-inspired-improvements/) | Accepted |
| ADR-9 | [クロスプラグインAPI](9-cross-plugin-api/) | Accepted（v0.1.1で実装） |
| ADR-10 | [オプトイン依存関係ソース推論](10-dependency-source-inference/) | Accepted |
| ADR-11 | [プラグインアダプタとしてのSorbet入力](11-sorbet-input-adapter/) | Accepted |
| ADR-12 | [dry-rbプラグインパッケージング](12-dry-rb-packaging/) | Accepted |
| ADR-13 | [`TypeNode`リゾルバプラグインフック + TypeScriptユーティリティ型アダプタ](13-typenode-resolver-plugin/) | Accepted |
| ADR-14 | [推論からのRBSシグネチャ生成と拡張](14-rbs-sig-generation/) | Accepted |
| ADR-15 | [アナライザーのRactorベース並行性モデル](15-ractor-concurrency/) | Accepted（フォークバックエンドがアクティブ；Ractorプールは見送り） |
| ADR-16 | [マクロ / DSL展開基板](16-macro-expansion/) | Accepted |
| ADR-17 | [プロジェクト側monkey-patchの事前評価](17-monkey-patch-pre-evaluation/) | Accepted（v0.1.13で実装） |
| ADR-18 | [基板の呼び出しサイトごとの戻り値型DSL](18-substrate-per-call-site-return-type/) | Accepted（v0.1.6で実装） |
| ADR-19 | [言語サーバーのパッケージング](19-language-server-packaging/) | Accepted（v0.1.6でLSP v1実装;v2 + フォローアップはv0.1.x全体で） |
| ADR-20 | [軽量高階多相性（Lightweight HKT）](20-lightweight-hkt/) | Accepted（部分実装） |
| ADR-21 | [Rubydex評価（基礎、バックエンド、ツール？）](21-rubydex-evaluation/) | Proposed |
| ADR-22 | [ベースラインメカニズム + プロジェクトオンボーディングSKILL](22-baseline-and-project-onboarding/) | Accepted |
| ADR-23 | [診断トリアージコマンド（`rigor triage`）](23-diagnostic-triage-command/) | Accepted（スライス1+2+3+4を実装） |
| ADR-24 | [implicit-selfメソッド呼び出し解決](24-self-method-call-resolution/) | Accepted（スライス4はゲート中；WD3のボディ内採用ゲートはADR-57により開かれた、2026-06-12） |
| ADR-25 | [プラグインが提供するRBSシグネチャ](25-plugin-contributed-rbs/) | Accepted |
| ADR-26 | [ActiveRecord relationの型付け](26-activerecord-relation-typing/) | Accepted |
| ADR-27 | [ツール配布・インストールモデル](27-tool-distribution-model/) | Accepted（部分実装；Nix flake＋コンテナイメージ＋機械可読インストールガイドを出荷、コピー＆ペーストできるCIワークフローテンプレートは[ADR-51](51-ci-diagnostic-output-formats/) WD6で着地——`docs/manual/ci-templates/`＋同梱の`rigor-ci-setup`スキル；自己完結型シングルバイナリは保留のまま） |
| ADR-28 | [パススコープのメソッドプロトコル契約](28-path-scoped-protocol-contracts/) | Accepted |
| ADR-29 | [ブラウザプレイグラウンド](29-browser-playground/) | Accepted（v0.1.10〜0.1.11でサーバーサイドプレイグラウンド；ブラウザ内`ruby.wasm`ビルド（オプションA／WD8〜WD10）はグリーンになり2026-06-14/15に出荷——`apps/rigor-playground/wasm/`、リリースタグでCloudflare R2に公開されドキュメントサイトが静的アセットとして配信） |
| ADR-30 | [`rigor-ffi`プラグインの形状](30-rigor-ffi-plugin-shape/) | Proposed（未実装） |
| ADR-31 | [貢献およびサプライチェーンポリシー](31-contribution-and-supply-chain-policy/) | Accepted（発効中） |
| ADR-32 | [オプトインプラグインとしてのインラインRBSコメント取り込み](32-rbs-inline-comment-ingestion/) | Accepted（v0.1.10で実装） |
| ADR-33 | [MCPサーバーパッケージング](33-mcp-server/) | Accepted（v0.1.10で実装） |
| ADR-34 | [トップレベルのunresolved implicit-self呼び出しはデフォルトで警告する](34-toplevel-unresolved-self-call-default/) | Accepted（v0.1.13で実装；ADR-29のPlaygroundデフォルト重大度の配線も出荷——そのサンドボックス設定が`severity_profile: strict`を設定する） |
| ADR-35 | [オーバーライドのシグネチャ互換性（リスコフのシグネチャ規則）](35-override-signature-compatibility/) | Accepted（スライス1〜4完了；スライス5は見送り） |
| ADR-36 | [マクロ基板のネストクラス生成ティア（Mangrove `Enum`）](36-mangrove-enum-nested-class-emission/) | Accepted（Slice A実装済み；`is_a?`網羅性は見送り） |
| ADR-37 | [プラグインのインターフェース分離（狭い拡張プロトコル）](37-plugin-interface-segregation/) | Accepted（スライス1〜3実装済み；同梱のウォーカープラグインはすべて移行済み；`flow_contribution_for`はADR-52 WD3に従い2026-06-11に削除） |
| ADR-38 | [プラグインが宣言する追加イニシャライザー](38-additional-initializers/) | Accepted（def形式は実装済み；ブロック形式は見送り） |
| ADR-39 | [プラグインは対象ライブラリの安全なメソッドを直接呼び出せる](39-plugin-target-library-invocation/) | Accepted（Plugin::Inflector＋3つのコンシューマーを移行；スライス3は見送り） |
| ADR-40 | [`config_schema`で宣言するデフォルト値（`{kind:, default:}`）](40-config-schema-defaults/) | Accepted（メカニズム＋13個のプラグインを`DEFAULT_*`イディオムから移行） |
| ADR-41 | [推論バジェットの設計（配線、ヒット時ポリシー、計測ゲート付きデフォルト）](41-inference-budget-design/) | Proposed（仕様テーブルは未配線；Layer 1のドキュメント衛生＋Layer 2の計測ゲート付き配線がキュー待ち） |
| ADR-42 | [プラグインが提供する二項演算子の戻り値型（coerce方向）](42-plugin-binary-operator-return-types/) | Proposed、低優先度（self／左辺オペランドのケースは`dynamic_return`経由ですでに動作、仕様で確認済み；強制方向は狭い偽陽性——最も安価な修正はWD-Dのエンジン緩和、精度はADR-20のHKTルート；需要ゲート付き） |
| ADR-43 | [RBS完全な祖先解決（許可リストによる継承メソッドのディスパッチ）](43-rbs-complete-ancestor-resolution/) | Accepted — 全面的に着地（WD1–WD6;`rigor check`がRubyサブクラスの継承された呼び出しを、アローリスト化されたRBS-completeな祖先（シード`Plugin::Base`）に対して解決するため、契約の誤用がSteepの自前ヘルパーFPの壁なしにスタンドアロンで警告される;プラグインlibツリーで正味FPゼロ;一律な解決はRailsコントローラーのFPを理由に却下;`verify` + CIで`make check-plugins`ゲート、効力を検証済み） |
| ADR-44 | [ディスパッチごと／ナローイングごとのアロケーションチャーン（Scope、CallContext）](44-dispatch-allocation-churn/) | Accepted — 着地済み: 約12段のボディスコープ`with_*`チェーンを単一の`Scope.new`に折り畳み（GC実行−29 %、診断結果バイト同一） + `owners_for`の共有空 + `CallContext`の位置引数`new`;ミュータブルプール型のScope/CallContextは**却下**（再入可能ディスパッチ→サイレントなナローイング破壊→偽陽性）;`ProjectScope`のフィールド再グループ化は優先度引き下げ（オブジェクトシェイプのベンチマーク:3〜24個のivarはすべて1個のオブジェクトをアロケートするため、数ではなくサイズを削減する） |
| ADR-45 | [変更なしプロジェクトの高速パス（実行結果キャッシュ）](45-unchanged-project-fast-path/) | Accepted — 着地済み。解析前の実行全体フィンガープリントは**不健全として却下**された（Punditのようなプラグインは解析*中に*プロジェクトファイルを読み込む→古いヒット、クロスプロセスの`pundit_plugin_spec`退行テストが捕捉）。健全な**record-and-validate**キャッシュ（`Cache::Store#fetch_or_validate` + `Descriptor#fresh?`）: 実行前に判明している入力をキーとし、実行が実際に読み込んだ依存集合とともに結果を保存し、次回の実行で再ダイジェストして検証する。変更のないMastodonの`app/models`（248ファイル）11.6秒→1.8秒（約6倍）、診断結果バイト同一;ゲートは`--no-cache`で実行する |
| ADR-46 | [ファイル間依存グラフによるインクリメンタル解析](46-incremental-dependency-graph/) | Accepted — 実装済み（`rigor check --incremental`、v0.1.17）。ADR-45の粗粒度な全体実行キャッシュのファイル単位の後継: ファイルごとに、他のファイルから何を読み取ったか（宣言、推論された戻り値のボディ、プラグインの読み取り）を`Scope`アクセサのチョークポイントを通じて記録し、`dependents`インデックスへ反転;編集時には`ΔF ∪ dependents[ΔF]`だけを再解析し、残りをクロスプロセスのディスクスナップショットから提供する。葉の`PostsController`編集→1ファイル;`Post`モデル編集→モデル + その呼び出し元。スライス1+2（ボディティア）が中核を出荷;スライス3（構造的ティアのネガティブ依存関係追跡、インクリメンタルなファイルの追加・削除を含む）とスライス4（シンボル粒度）はランディング済み。Rigor自身の`lib`（262ファイル）で計測: ウォーム変更なし0.75 s vs全体解析7.2 s（約9.6倍）、1ファイル葉編集1.15 s（約6.3倍）、診断結果は同一。健全性は必須の`--verify-incremental`バイト同一クロスチェックによって担保され、`make check-incremental`としてCIに組み込まれている |
| ADR-47 | [ナローイング駆動の節到達可能性（`flow.unreachable-clause`）](47-narrowing-driven-clause-reachability/) | Accepted — WD1 + WD2 + WD3a実装済み + WD4スイープを実施（v0.1.17;16コーパスのFPスイープでゼロ発火→balancedは`:info`のまま）。Rigorの既存の2つの`if`/`unless`到達可能性ルール（`flow.unreachable-branch`はリテラルのみ + `flow.always-truthy-condition`は推論された定数）を、Elixir v1.20の冗長節報告に触発されて`case`/`when`節へ拡張する。フローエンジンは既に`case`を絞り込んでいる（`eval_case_when_branches`は`Narrowing.case_when_scopes`を通じて`falsey_scope`を`when`分岐にまたいで通す）;節が到達不能なのは、計算された`body_scope`が対象を`bot`に絞り込む（節ごとのdisjointness）か、入口の`falsey_scope`が既に`bot`な対象を持つ（先行網羅）ときに限られる。`always-truthy-condition`のFPエンベロープを再利用する（対象は絞り込まれなければならない、ループ/ブロックをスキップ、防御的なシェイプをスキップ、`Dynamic[T]`では決して発火しない）;`in`/パターン節は`InNode`網羅性（ADR-36の隣接）の後ろにゲートする;デフォルトオン前のコーパスFPゲート |
| ADR-48 | [Struct / Data値畳み込み（メンバーシェイプキャリア）](48-data-struct-value-folding/) | Accepted — `Data.define`スライス1〜4実装済み（v0.1.17）。2つの新しいキャリア（メンバークラスキャリア`Type::DataClass`＋メンバーインスタンスキャリア`Type::DataInstance`。HashShapeのような構造だが名前的/クラスタグ付き）＋`DataFolding`ディスパッチ層＋クロスファイルの`Scope#data_member_layouts`サイドテーブルにより、`Point = Data.define(:x, :y); Point.new(1, 2).x`が3形式すべて（定数代入形式/`class X < Data.define(...)`サブクラス形式/ベアローカル形式）、位置引数＋キーワード引数の構築、および`[]`/`to_h`/`deconstruct`/`deconstruct_keys`/`members`/`with`の各プロジェクションで`Constant[1]`に畳み込まれる。既存の`ScopeIndexer`の`data_define_call?`/`meta_member_names`認識（メンバーの*存在*のみを登録していた）を再利用。スライス4（ブロックボディ堅牢化）：クラスボディがリーダーを再定義しているメンバー（`def x`）は読み取り時に畳み込まなくなった（`Scope#user_def_for`経由の実defノードでゲート）。値アクセサはリーダーをバイパスし引き続き畳み込む。精度追加のみ——診断なし、偽陽性のサーフェスなし、不確かな場合はクラスNominalに劣化する。`Struct`は変異健全性のストーリー（セッター/`[]=`がインスタンスのメンバーマップを無効化する。`Data`の凍結インスタンスがゆえに最初）の背後で先送り。残余（需要依存）：ベアローカルのブロック形式のパリティ＋Structフォローアップ。フェーズ5の`struct-encoding-coverage`監査に基づく |
| ADR-49 | [ADR執筆ガイドライン（必要十分なADRのためのルーブリック）](49-adr-authoring-guidelines/) | Accepted — 発効中（生きているルーブリック）。メタADR：各ADRの**アーキタイプ**（熟慮型/機械的・方針型/評価・提案型）と**ステークス**（可逆性×ブラストラジアス×偽陽性エンベロープ）をタグ付けし、次に**8つの評価軸**（意図・決定基準・実行可能性/コードアンカリング・制約/ガードレール・検証可能性/エビデンス・ステータス忠実度・コネクテッドネス・エコノミー/比例性）を採点する。アーキタイプが免除する評価軸はゼロ点にせず分母から除外する。目標は*必要十分*な情報——エコノミーは双方向（薄すぎも膨らみすぎも罰する）だ。過剰ペナルティ回避ルール：機械的/半自動ADRは意図/基準で免除される（ハイステークスのものは決して免除しない。そこでは薄い意図がヘッドラインの発見だ）。キャリブレーションセット：ADR-43/ADR-46（熟慮型・high）＋ADR-40（機械的、適切に簡潔） |
| ADR-50 | [リリースエンジニアリングと安定化戦略（v0.2.0 → v1.0.0）](50-release-engineering-and-stability-strategy/) | Proposed — Rigorをv0.1.xの機能開発ラインからエンタープライズ用途に耐える安定製品へ移行させる戦略、PHPStanを参照モデルとする。**v0.2.0＝リリースエンジニアリングの試行**（機構＋マイナーノンブレークの誓約をリハーサルとして）、**v1.0.0＝ハードな契約フリーズ**（ADR-25/32対ADR-37のフリーズタイミング不整合を1.0支持で解決する）。5本の柱：（1）試行してからフリーズする段階化。（2）互換性＝列挙された公開サーフェスをフリーズ、エンジン内部は自由。（3）診断*出力*は非契約（本物の強化はマイナーで流れ、ベースラインが吸収する）だが*新しい必須の規律*はBCであり、卒業（次のメジャーで約4週間のソーク後にデフォルトオン、BC-readiness駆動。1.0以前はマイナーでリハーサル）まではデフォルトオフで、PHPStan方式の**inspectableなbleeding-edgeオーバーレイ**（gemでバージョン管理されるデフォルトオーバーライドのセット。`show-bleedingedge`diffコマンドで表面化し、`bleeding_edge:`でフィーチャー単位にgranularに採用する）の背後で出荷される——安定した語彙（ルール識別子、抑制マーカー、ベースライン/`severity_overrides:`キー、bleeding-edgeのfeature id）は契約のまま。（4）Mastodon/Redmine/GitLabコーパスに対するコミットされた`make bench-perf`＋CI perfゲート（許容帯域内で格納されたベースラインに対するwall/allocations/peak-RSS）。（5）サポート＝最新＋直前マイナー、1.0以降はPHPStanの`1.x`デフォルトブランチモデルへ切り替え、latest-Ruby専用と整合させる（バックポートは自ラインのRubyのピンを保つ）。WD6がリリース受け入れゲートを定義する。いくつかのWDは批准前の調整に開かれた具体的な形状を提案している |
| ADR-51 | [CIネイティブな診断出力フォーマット](51-ci-diagnostic-output-formats/) | Accepted — 部分的に実装済み（v0.1.18）。既存の診断ストリームの6つのCIネイティブなレンダリングを`rigor check --format`の背後に置く。**`sarif`**（SARIF 2.1.0、クロスプラットフォームのアンカー——GitHubコードスキャニング＋任意のSARIFツール＋reviewdog `-f=sarif`）、**`github`**（GitHub Actionsの`::error file=…,line=…::`ワークフローコマンド→インラインPRアノテーション、アップロードステップ不要）、**`gitlab`**（GitLab Code Quality／CodeClimateサブセットのJSON→MRウィジェット）、**`checkstyle`**（Checkstyle XML→reviewdog `-f=checkstyle`が*任意の*reviewdogレポーター——GitHub PRレビュー／GitLab MR／Gerrit／Bitbucket／Gitea——とJenkinsへポストする）、**`junit`**（JUnit XML→GitHub／GitLab／CircleCI／Jenkinsのテストレポート）、**`teamcity`**（TeamCityのインスペクションサービスメッセージ）。それぞれは`--format json`が公開しているのと同じ`Analysis::Diagnostic`フィールドの上に乗る純粋なプレゼンテーション層であり、新しい解析はない。`--format`の値となる基準は*CIプラットフォームがそれをもとにストリームをインラインでレンダリングすること*だ（だからCSV／汎用の形状はスコープ外——`json`がそれらに対応する）。WD1は*単位コストあたりのリーチ*に基づいて広いリーチのフォーマットを加える。比較対象＝PHPStan（github／gitlab／checkstyle／junit／teamcityを出荷するが**SARIFはない**——RigorはSARIFを先頭に据え、PHPStanのギャップを突く）。GitHubの*デフォルト*サーフェスはSARIFではなく`github`アノテーションだ（SARIFアップロードはコードスキャニング＝プライベートリポジトリではGHASを必要とする）。WD2は6つすべてのフォーマットにわたるseverity＋識別子の**契約テーブル**を固定する。WD3はGitLabのfingerprint（`(path, rule, line, column, message)`のSHA256——安定しており、インデックスベースではない）。WD4は終了コードを不変のまま残す（`>`リダイレクトによるファイル出力＋`if: always()`のアップロード）。WD5はすべての診断をJUnit `failure`にマップする（ゲートではなく可視化——severityは`type`に保持）。**WD7＝ランタイムCI自動検出**（`Rigor::CLI::CiDetector`、PHPStanの`CiDetectedErrorFormatter`をモデルにしている）。デフォルトの`text`出力に対して、検出された**ファーストクラス**のCIがそのネイティブな形を自動出力し（GitHub Actions→`github`、TeamCity→`teamcity`、人間向けログの上に重ねて）、GitLabは`--format gitlab`をヒントし、**セカンドクラス**のCI（CircleCI／Jenkins／Travis／Azure／Bitbucket／Buildkite／Drone／…）はreviewdog／`junit`をヒントする。textを補強するだけで（明示的な`--format`は手つかず）、ヒントは診断がある場合にのみ発火し、`--no-ci-detect`／`RIGOR_CI_DETECT=0`でオプトアウトできる。新しい出力フォーマットは[ADR-50](50-release-engineering-and-stability-strategy/) WD1の下で公開契約だ。WD6は[ADR-27](27-tool-distribution-model/) § WD3のキューに残されたコピー&ペースト用CIセットアップテンプレート＋同梱の`rigor-ci-setup`スキルも出荷する。先送り: `--output FILE`、reviewdogネイティブの`rdjson`、よりリッチなSARIFルールメタデータ |
| ADR-52 | [コンパイル済みプラグイン貢献ディスパッチ](52-compiled-plugin-contribution-dispatch/) | Accepted — スライス1＋2＋3一部＋4一部を2026-06-10/11に実装、コーパス／スペックにゲートされバイト単位同一;スライス3のrigor-activerecordは**ブロックされている**（そのクラス側ディスパッチはレシーバー型ではなくAST／`self_type`をキーとする——移行を試みたが差し戻した）;スライス4（実行時メソッド名のcallable）はそのエンジン＋lisp-eval／patternの例＋rigor-sorbetを着地させた（2026-06-11: アサーション語彙 ∪ `:absurd` ∪ `Catalog#method_names`上の`dynamic_return methods:` callable、`T.bind`のファクトを`type_specifier`へ分割、strapおよびdependabot-coreコーパスでバイト単位同一、dependabot-coreのウォール時間1262.6s→33.4s約38倍）;スライス5a（ファイルごとの`file_methods:`ゲート——パスを受け取るcallable、（rule, path）ごとにメモ化、`methods:`と排他、nilパスはフェイルクローズ）を2026-06-11に着地させ＋rigor-rspecのlet束縛ルールを移行（デモ＋GitLabコーパスでバイト単位同一）、rigor-activerecordも2026-06-11に同じ実行時`methods:`ゲートで移行（レシーバーゲートのブロッカーは名前ゲートには当てはまらない——ブロックはAST／self_type解決を保つ;新しいゲート形式は不要; GitLabコーパスでバイト単位同一）、こうして5つのレガシーユーザーがすべて移行済み;スライス5bを2026-06-11に着地させ（フックを削除——定義しているプラグインに対するロード時のArgumentError＋CHANGELOGの移行ノート）、スライス6を2026-06-11に着地させた（エンジン所有の`Plugin::NodeRuleWalk`、ファイルごとに1回のウォーク、プラグインメジャー順をバイト単位同一に保持）——ADRは完了だ。[2026-06-10のプラグインアーキテクチャ構造監査](../notes/20260610-plugin-architecture-perf-audit/)に基づく。基準: 呼び出し元ごと／defごと／ファイルごと／ノードごとのすべてのプラグイン照会が、エンジンがすでに保持するキー（メソッド名のSymbol、レシーバークラス名、ファイルパス、ノードクラス）によって、**レジストリ構築時に実行ごとに1回コンパイルされる**構造を介してゲートされる——プラグインコードは候補がヒットしたときにのみ走る;そのキーを宣言できないケイパビリティは、ゲートされていないフックの許可証ではなく、修正すべきDSL語彙のギャップだ。**スライス1（WD1）**は`ContributionIndex`をコンパイル済みのcontributionテーブルへ拡張する（`type_specifiers`＋`methods:`でゲートされる`dynamic_returns`のメソッド名転置インデックス、動詞をキーとする`block_as_methods`、`open_receivers` Set、祖先メモ化された`owns_receivers`／`additional_initializers`、パスごとの契約メモ、`diagnostics_for_file`オーバーライドビット）＋6箇所のエンジン呼び出し元の再配線（GitLabゲートが名前なしの`&:symbol`ノードのリグレッションを捕捉・修正した）。**スライス2（WD2の静的ゲート）**は`dynamic_return receivers:`をオプションにする——レシーバーキャリアが名前的クラスではなくリファインメントであるプラグインのために、ルールが`methods:`単独でゲートできる; `rigor-units`の例を移行した。**スライス2の実装中に行った監査の訂正**：監査のスライス2の対象は両方とも誤りだった——activesupport-core-extは`flow_contribution_for`をまったく出荷せず（純粋なRBSバンドル）、sorbetのカタログパスは任意の`def`名にマッチする（静的ではなく実行時の集合）;静的ゲートに適合する本番プラグインはなく、unitsがその最初の消費者だった。WD3（現在のスライス3〜4）は実行時のレシーバー／メソッド集合＋ファイルごとのcallable形式を加え、残りのユーザー（activerecord／activestorageのレシーバー集合、sorbetのカタログ名集合、rspecのファイルごと、configゲートされた例）を移行し、それからレガシーフックを**削除する**（意図的な1.0前のBC破壊——ADR-50の凍結前なら安い）; WD4はプラグインごとのノードルールASTウォークをエンジン所有のファイルごとに1回のウォークへ畳み込む; WD5は呼び出しノードごとの結果キャッシュを棄却する（スコープ依存→偽陽性のリスク）; WD6はすべてのスライスをバイト単位同一の診断（Mastodon 6プラグイン／GitLab 11プラグインのコーパス）＋stackprofのデルタ＋`make bench-perf`にゲートする。凍結されたテーブルはRactor共有可能だ（ADR-15フェーズ4へ供給する）;呼び出しごとの表面についてADR-37の分離の弧を完成させる |
| ADR-53 | [Scopeの発見インデックス分離 + チェックルールウォークの統合](53-scope-discovery-index-separation/) | Accepted — **トラックA完了**（A1 `031f161e` + A2 `063823e4`、2026-06-10/11。これを着地させたことで予測したバグクラスが2回顕在化し修正された — ADR-44の手書きコピーのbodyスコープコンストラクタ2つがどちらも後から追加されたテーブルを静かに取りこぼしていた）。**トラックB完了**（B1+B2 `6858872c` 2026-06-11、B3+B4 2026-06-13: シャドウハーネスでゲートされたウォーク統合が着地した — 5つの組み込みのファイルごとのASTウォーク（メインパス + 4つのコレクター）にプラグインウォークを加えたものが、いまやADR-52 WD4の`Plugin::NodeRuleWalk`の内側で駆動される`CheckRules::RuleWalk`を介して、エンジン所有の1つのファイルごとの走査に乗る）。[2026-06-10のlib/rigorアーキテクチャ再レビュー](../notes/20260610-lib-rigor-architecture-rereview/)（フェーズ4、設計判断を要する2つの所見）に根拠を置く。**トラックA**は14個のシード時の発見テーブル（`discovered_*`、`declared_types`、`class_ivars`/`class_cvars`/`program_globals`、`data_member_layouts`）を`Scope`から1つのイミュータブルな`Scope::DiscoveryIndex`へ抽出する — メンバーシップ基準: *いかなるフロー遷移もそのフィールドを決して変えない*（試金石はすでにコードにある: `Scope#==`がそれを無視し、`#join`がそれを検査せずにコピーする）。リーダーはデリゲートとして`Scope`に留まり（プラグインに影響なし、ADR-46の`DependencyRecorder`チョークポイントを保つ）、テーブルごとの`with_discovered_*`ライターは`with_discovery`へ畳み込まれる（1.0前のクリーンアップ、ADR-52 WD3の姿勢）。ADR-44が*アロケーション*のレバーとして格下げした再グルーピングを、*境界/ボイラープレート*の基準で再採用する（新しい副テーブル: 7つの編集箇所 → 1; `rebuild`の24個のkwarg → 11）。性能は中立と主張され、`make bench-perf`でゲートする。**トラックB**は`CheckRules`の5つのファイルごとのASTウォーク（メインパス + 4つのコレクター）をエンジン所有の1つのルールウォークへ統合する（ノードクラス → コレクターフック — 組み込みに適用したADR-52 WD4モデル;プラグインウォークと収束 → ファイルごとに合計2ウォーク）、**ハードな前提条件: シャドウラン等価性ハーネス**（レガシー対マージ、self-check + Mastodon/GitLabコーパス上で同一の結果）— コレクターはそのまま移植され、走査だけがマージされる。すべてのスライスはバイト単位同一の診断 + `make bench-perf`でゲートする |
| ADR-54 | [キャッシュのスリム化: definitions-blobの廃止、ペイロード圧縮、デフォルトのエビクション](54-cache-slimming/) | Accepted — WD1〜WD4を2026-06-10に実装。[2026-06-10のキャッシュディスク + ウォームロード監査](../notes/20260610-cache-disk-runtime-audit/)に根拠を置く。基準: *キャッシュ階層がそのバイト数を稼ぐのは、ウォームパスにおいて次に安い階層からの再計算を上回る場合に限る*（コールド再構築に対してではなく、自身の上流キャッシュに対して計測する）。WD1は`rbs.instance_definitions` / `rbs.singleton_definitions`のディスクblobを廃止した — キャッシュ済みenvがあれば差し引きマイナスと計測された（366 msの`Marshal.load`対137 msのenvからの全構築; 23.4 MB/プロジェクト）: ディスパッチパスは既存のプロセスごとのメモの上でクラスごとに遅延化し、ADR-15のprewarm/Reflectionの消費者はキャッシュ済みenvから構築した完全なテーブルをeagerに保つ。WD2はフォーマットバイトのbumpの背後ですべてのエントリーの値ペイロードをzlib deflateする（env blobは1.76 MB = 生の11.0 MBの16%に着地する; inflateはそれが先行する`Marshal.load`より1桁下;旧フォーマットのエントリーは静かなミスとして読まれる — 移行コードなし）。WD3は`cache.max_bytes`を256 MBにデフォルト設定するので`evict!`が恒久的なno-opではなくなる（rigorリポジトリ自身のキャッシュは約2 MBのアクティブ集合に対して約180 MBのオーファンを溜めていた — キャップ付き試験実行がまさに古いエントリーを刈り取った;明示的な`null`は上限なしを復元する）。WD4はローダーごとに`RbsDescriptor.build`をメモ化する。エンベロープ: 約33.7 MB → 約2 MB/プロジェクト（−94%）;定義に触れるウォーム実行は最大で約550 ms / 160万アロケーションを節約する。ADR-7の§ Slice 6-Dを部分的に置き換える（定義blobの半分;クラスごとのディスクエントリーの棄却は立つ）。プロジェクト横断の共有キャッシュルートは延期（WD1/WD2後の重複は約1.7 MB × N）; ADR-45の`fresh?` mtime高速パスは健全性を理由に棄却。スライスごとに診断同一の実行（self-check `--no-cache`/コールド/ウォーム + Mastodonコーパス）でゲートする |
| ADR-55 | [再帰メソッドの戻り値型精度（定数引数の有界アンロール + 不動点の戻り値型サマリー）](55-recursive-return-precision/) | Accepted — スライス1（フューエル付き定数引数アンロール）とスライス2（不動点の戻り値型サマリー）はともに2026-06-11に実装。ADR-41 WD5の先送りされた精度アンロールを具現化し、WD5が想定しなかった機構を加える: 再帰戻り値型サマリー上の`bot`からのKleene不動点で、深さ1ガードのサイクル内`untyped`を置き換える。これにより再帰的な`String`ビルダーは`String`を返し、`String \| Dynamic[top]`ではなくなる（`factorial(x: Integer) → 1 \| Integer`、`Dynamic`寄与なし）。スライス2はサマリーテーブルをプレーンな`(receiver, method)`シグネチャ（拡張アンロールフレームと共有）でキー付けし、反復をシグネチャごとの*参照済み*フラグでゲートし、`BudgetTrace::RECURSION_FIXPOINT_CAP`を加える; WD4のクランプ対サマリーの合成は狭められた（スライス1のクランプは素の`untyped`に留まる — サマリーの消費者ではなく健全性のバックストップだ — 進行中のサマリーはKleeneの下界なので;ガード/フューエルのパスだけがそれを参照する）;相異なるシグネチャにまたがる相互再帰は`Dynamic[top]`へ健全にデグレードする（停止が荷重を担う性質だ）。Mastodonの`app/models` + hamlの`lib`はともにスライス2以前のベースラインとバイト単位同一。基準: *再帰戻り値の精度機構は、その枯渇パスが現在の広げとバイト単位同一である場合にのみ許容される* — フューエル切れ / 反復上限（3、最終ラウンドは収束を強制するため`Constant→Nominal`を広げる） / 値の膨張のいずれも現在の`untyped`へデグレードし、ハードかつ設定不可の停止上限の内側にある（ADR-41 WD4）。スライス1はガードキーを`(receiver, method, argument values)`へ拡張するのは、束縛された引数すべてが値ピン留めのときに限る、フューエル32 + 値サイズ上限（ADR-24 WD5の型キー棄却を保つ — 型ではなく値、フューエル有界）; `Constant[120]`の帰結は明示的に低価値で、このスライスはガードキーの一般化 + `BudgetTrace`配線として地位を得る。支配的なクランプ（WD1、コーパスFAIL修正2026-06-11）: アンロールは*完全に値ピン留めされた*結果しか顕在化させてはならない — ガードされた再入はその本体が非ピン留めのクランプを`untyped`へ畳み戻し、値ピン留めself呼び出しの採用は進行中のアンロールに限定される;最初のランディングの無差別な採用は本体評価器のブロック内`return`の盲点を3つの新しいコーパス診断（mastodonの`account.rb`/`base_action.rb`、hamlの`children_compiler.rb`）として漏らした、いまはスライス1以前のベースラインとバイト単位同一。精度加算的のみ: 新しい診断なし;ゲート = `make verify` + `make bench-perf`ニュートラル + コーパス診断の差分がゼロか除去のみ（hamlの421回のガードヒットがコストのストレスケースだ）。クロスコールのサマリーメモ化 + 設定可能性 + ADR-41 WD2の`static.*`枯渇診断は先送り |
| ADR-56 | [ブロックがキャプチャしたローカルのライトバックとループ本体の不動点（ミューテーション効果の健全性）](56-block-captured-local-mutation/) | Accepted — スライスA（非脱出ブロックがキャプチャしたローカルのライトバック）は**2026-06-11に実装**（`record_closure_escape_if_any`の後の`StatementEvaluator#write_back_block_captures`、スライスBが再利用する共有の`Inference::BodyFixpoint`上限3ヘルパー、`captured_local_writes`を5つの書き込み形すべてへ拡張、`Combinator.widen_value_pinned`を昇格 + `Refined`/`IntegerRange`の広げ、`BudgetTrace::BLOCK_WRITEBACK_CAP`;ゲートはグリーン — コーパスは除去1/新規ゼロ、perfニュートラル）;スライスB（ループ本体の不動点）は**2026-06-11に実装**（`eval_loop`は歴史的な単一パスのjoinをベースとして保ち、本体が再束縛するローカルについて`BodyFixpoint.converge`の結果をオーバーレイする; `loop_body_local_writes`は既存（述語後の種）対本体先（0反復パス向けの`nil`の種）に分割する; `loop_body_exit_bindings`は述語のループ入りエッジ（`while`→真/`until`→偽）を反復ごとに再適用する;書き込みなしループはバイト単位同一に留まる（高速パス）;ゲートはグリーン — Mastodonの`app/models` 5/5 + hamlの`lib` 13/13がバイト単位同一、kramdownの`lib`は除去2（`until`内の`html.rb:455` `stack.pop`がもはや誤ったnil畳み込みをしない）+ 安定箇所での5つのメッセージ言い換え、本物の新発火ゼロ;スライス内の盲点修正が1つ: 本体先の`nil`の種は本体の再評価へオーバーレイされず、join構成要素としてのみ保たれる、さもなくばエンジンが分岐へ通さない条件形の代入を越えて`nil`が漏れる）。**精度追加ではなく健全性修正**: エンジンはすべてのブロック本体を評価するがその出口スコープを破棄するので、`result = 1; 1.upto(6) { result *= i }; result`は`Constant[1]`と型付けされ（ランタイムは720）、すべてのキャプチャ書き込み形（`=`/`+=`/多重代入）が`:non_escaping`ブロックに対して落とされる; `while`は単一の本体パスをjoinする（`1 \| 2`、`4, 8, …`を取りこぼす）。これは仕様のキャプチャされたローカルのMUST（`control-flow-analysis.md` § Fact stability）に違反する。基準: *ブロック/ループ本体が再束縛しうるキャプチャされた外側のローカルは、呼び出し前の束縛を未変更のまま決して保持してはならない* — 継続 = 事前状態を有界不動点の事後状態とjoinしたもの（上限3、最終ラウンドの`Constant→Nominal`広げ、非収束時にローカルごとの`Dynamic[top]` = 既存の脱出ブロックのフロア;両スライスで1つの共有ヘルパー、ADR-55のパターン）。脱出/不明のパスと書き込まれないローカルは不変。ゲート（WD4）は*ゼロ差分ではなく裁定*: 訂正された定数は本物の診断を発火しうる — 新しい発火はどれも本物として残すか人工物として修正するか裁定される;除去は期待される勝利だ;手でプローブした弁別形がコーパスと並んで必須（ADR-55の教訓）。スライスC（レシーバー内容の要素型join、WD2.5）は**2026-06-12に実装**（スライスA/Bのライトバックはローカルの*再束縛*をカバーしたがレシーバーの*内容*変異はしなかった: `out = [0]; [1,2,3].each { \|x\| out << x }`はランタイムの`[0,1,2,3]`に対し`Array[0]`と型付けされた — 不健全で、`out.first.zero? → true`という誤った畳み込みを派生させた; `content_writeback_block_captures`（ブロック）、`loop_content_writeback`（ループ、高速パス + 不動点）、`each_with_object_return`（B3）が、追加/格納された要素/キー/値の型を`MutationWidening.join_array_content`/`join_hash_content`を介して継続コレクションへJOINし、スライスA/Bのキャリア広げを再利用する; `CONTENT_ADDERS`はArray/Hash/Stringの変異子 + インデックス書き込み形をカバーする（`h[k] \|\|= v`がもはや`h`を空の`{}`のまま残さない）; Stringのアキュムレータは`String`へ広がる;ゲートはグリーン — Mastodonの`app/models`はバイト単位同一、hamlの`lib`は除去1（`parser.rb:746`の`dynamic_attributes == "{}"`がもはや誤った常に偽でない）、kramdownは1つのメッセージ言い換え、本物の新発火ゼロ; `loop_body_fixpoint`の`acc.push(m)`フィクスチャは`Array[Dynamic[top]] \| []` → `Array[Integer]`へ締められた）。無差別な「yieldがすべてを無効化する」と1回限りの出口スコープ採用は却下;メソッドごとの反復回数サマリーは先送り |
| ADR-57 | [implicit-self呼び出しの戻り値採用ゲートを開く（ADR-24 WD3の再検討）](57-self-call-return-adoption/) | **Accepted — ゲートは2026-06-12にオープン（スライス1〜3）；オーバーライド可能メソッドの採用ゲートを2026-06-13に追加（追補を参照）**。素朴な手続き型Rubyに残っていた最大のDynamic源を閉じるADR-24 WD3の再検討（`def outer = helper(3)` → かつては`Dynamic[top]`、いまは呼び出し先の推論された戻り値；ADR-55のfixpointサマリーが本体内の呼び出し地点へ届くのも解放する）。WD1はADR-55/56以降のエンジンで無条件オープンを計測し、+25件のセルフチェック発火だった。判別基準＝*ゲートは裁定された発火クラスごとにオープンし、丸ごとではない* ── 各件をgenuine（着地して勝ち筋となる）かアーティファクト（採用された型が誤り＝評価器の盲点で、根本修正する）に分類する。スライス1は支配的なアーティファクト（末尾のみの本体評価器が明示的／ブロック内／多値の`return`を取りこぼしていたもの、25件中15件）を修正した。スライス2〜3は残りを修正した: エスケープするブロックキャプチャ内容の床＋そのレシーバーチェーンおよびメソッド境界をまたぐ（`build_option_parser(opts).parse!`）変種、FP安全なオプショナルタプルスロット分解のソフト化（ゲートクローズ時のhaml FPを2件除去）、live Stringのfold-guard、過度に厳格な自作RBSの修正2件。残余はすべてgenuine-or-win（セルフチェック＋プラグインセルフチェックは発火ゼロ；haml/kramdownは同一；Mastodonは既存エラー1件がより精密なメッセージに）であり、`adoptable_self_call_result?`は削除され採用は無条件になった。コストはコールド`--no-cache lib`の実時間で約+12%（呼び出し先本体の再型付けに内在）；健全な呼び出しごとの戻り値結果メモはキュー入りの性能フォローアップ（ADR-52 WD5／ADR-24 WD5が素直なキャッシュを禁じている）。**WD3（2026-07-10）はモジュールシングルトンの解決を完成させた**: tier 4は型付けの半分（モジュール定数の呼び出しが呼び出し先の`def self.x`本体を再型付けする）を着地させていたが、プロジェクト全体の発見シードは依然としてクラスしか登録しなかったので、モジュール定数は*他の*あらゆるファイルから`Dynamic[top]`と型付けされていた——実アプリが消費するファサードのシェイプ（`Feature.enabled?`＝GitLabで695件の未保護サイト）だ。モジュールは今やクラスと同じ条件でシードされる;除外が述べていた危険（`Kernel.select → Array[String]`）は再現しない——素のモジュールで解決する28個のObject/Kernel/Module/Classメソッドのうち26個は正しく、正しくない2つ（`M.new`／`M.superclass`）はランタイムで例外を起こすコードだけを誤型付けする。WD2に従って裁定: 潜在的な厳格さのセルフチェック発火1件を根本修正（`DiagnosticFormats.render`を全域化）;haml/kramdown/liquid/rgl/Mastodonはバイト同一;Redmineの1件の`possible nil receiver`は独立したアーティファクトクラスに辿り着いた——ユニオンレシーバーは`rigor:v1:predicate-if-true`ファクトを決して受け取らないので、ActiveSupportの`present?`ガードがnilをナローイングしない——それは直後のユニオンアーム述語極性スライスでその根から修正された（それはWD3の発火に加えてRedmine 3件とGitLab 16件のFPを除去、新規ゼロ）。WD3の成果: GitLab `lib`の保護0.2457 → 0.2757（+2,812保護サイト、分母同一）、`make bench-perf`はグリーン |
| ADR-58 | [インスタンス変数のフィールド型付け: 宣言由来のnilポリシー、同種書き込みの読み取り、コンストラクタの確定代入](58-ivar-field-typing/) | Accepted — WD1（束縛出自サブセット）＋WD2（already-realizedとして解決 ── フローインセンシティブな書き込みユニオンが既に`join(writes) \| nil`を生む）＋WD3（無条件の同一クラス呼び出しを通したコンストラクタの確定代入、ipaddrの`@mask_addr`クラスタを解消）を2026-06-12に実装；WD1bのメソッド戻り値経由の残余はキュー入り。アルゴリズムコーパス調査は、**possible-nilエラーの94%（109/116）がノードフィールドのivar読み取り**（`current.next`、木の不変条件下での回転読み取り）であると計測した ── `attr_accessor`＋コンストラクタの`@x = nil`がシードする型により読み取りは`Dynamic[top] \| nil`となり、フローが証明できない不変条件で守られた読み取りで`nil`が発火する。判別基準: *ivar由来のオプショナリティが`possible-nil-receiver`を発火するのは、nilがフローライブ（メソッド内で読み取りに到達する経路上で代入／観測されている）であるときのみ；宣言由来のnil（クラスivarインデックス経由のコンストラクタシード）は表示される型に留まるが診断の燃料にはならない* ── 動作しているプログラムの不変条件は前提とされる（ADR-5；ADR-57スライス3の分解ソフト化が先例）。スライス計画（実現どおり）: WD1の出自ゲート付き発火（109件のFP修正）→ WD2の同種書き込みフィールド読み取り（`current.next → Node \| nil`；着地時にフローインセンシティブな書き込みユニオンで既に実現済みと判明、コーパス収量～ゼロ）→ WD3の同一クラス呼び出しを通したコンストラクタの確定代入（ipaddr/uri/ldap標準ライブラリクラスタ）。メソッド間の確定代入のみを表看板にする案は棄却（ノードフィールドは真にnil可能）；Dynamicを含むユニオンの抑制を基準にする案も棄却（WD2以降は成立しない；出自が安定した規則）。ゲートはADR-56 WD4＋必須の弁別プローブ（本物のローカルな`@x = nil`読み取りはMUST発火し続ける）に従う |
| ADR-59 | [スペックのアサーションは実装のシグネチャではない](59-spec-assertions-are-not-signatures/) | Accepted（強形は棄却） — `expect(foo(1)).to be_a(String)`が`foo: (Integer) -> String`を生み得るという創設期のアイデアを引退させる。判別基準: *アサーションは入力ごとの証人であって普遍的な主張ではない ── 証人はシグネチャを両端とも間違った側から押さえる（観測された戻り値／例の引数は下界だが、ADR-5は上界を必要とする）ため、スペック由来の主張が着地してよいのは、間違っていても診断を発火させ得ない場所に限られる*: `Dynamic`の内部、sig-genのレビューチャネル、あるいは推論に*対する*検査として。障壁はアーキテクチャではなくポリシーである（factorybotの`producer`パターンならこれを担えるだろう）。需要ゲート付きの弱形を3つ記録する: `spec.impossible-assertion`の矛盾診断（マッチャーによるナローイング → `bot`）、`spec_return_claims`ファクトからの`Dynamic[top]`→`Dynamic[T]`内部ヒント、ランタイムTracePoint → 別gemでのsig-gen。READMEのコミットメント#2も同じ変更で文言を改める |
| ADR-60 | [フリーズ前のプラグイン契約統合](60-pre-freeze-plugin-contract-consolidation/) | Accepted — ADR-50 v1.0サーフェス凍結前の最後の窓でのBC破壊、2026-06-13のプラグインインターフェースレビューから。判別基準: *サーフェスが凍結に入れるのは、配線済みで、一貫した名前を持ち、ドキュメントどおりに使ったとき気づかぬうちに誤った結果を生み得ない場合に限る*。WD1は一度も配線されなかった`external_files:`マニフェストフィールド（＋`Macro::ExternalFile`；ADR-16のTier Dはそのスキャナとともに需要ゲート付きで戻る）を削除する。WD2は`BlockAsMethod`の`verbs:`→`method_names:`と`NestedClassTemplate`の`name_arg_position:`→`symbol_arg_position:`をリネームする（シムなし）。WD3は構造的な古いキャッシュのハザードを修正する: `cache_for`がADR-45の`fetch_or_validate`へ移り（依存ディスクリプタはプロデューサーブロックの実行**後に**記録されるので、ブロック内の`IoBoundary`読み取りが常にキャプチャされる）＋`Cache::Descriptor::GlobEntry`による新しい`producer watch:`のglobカバレッジ（1エントリーで内容／追加／削除をカバー）；「`cache_for`の前にプライムする」イディオムと手組みの`glob_descriptor`サイトは削除される。WD4の加算的ヘルパー（`read_fact`／`producer_value`＋`producer_error`／`diagnostics_for`）は最終的なオーサリングイディオムを定め、12個の`*_or_nil`メモ＋約23個の違反マッピングサイトを移行する。再審理に抗して記録したKeep判定: `dynamic_return`/`type_specifier`の分離、`diagnostics_for_file`、`config_schema`の二重文法。ゲート＝ADR-52スライス5bの先例＋プロセス間の監視ファイルキャッシュのリグレッション＋コーパスがバイト単位で同一 |
| ADR-61 | [エージェントに優しい診断統計（構造化セレクタ軸）](61-agent-friendly-diagnostic-statistics/) | Accepted — 2026-06-13に実装；精度に対して加算的（新しい診断なし、バイト単位で同一）。AIエージェント／`jq`が、メッセージ文字列をパースせずに実行結果のクラス／メソッド統計を算出できるようにする。基準A: *メッセージ文字列は提示であって契約ではない ── エージェントが集計または分岐の対象とするデータは構造化されたJSONフィールドでなければならず、`message`からしか復元できないものであっては決してならない*（ADR-23 WD3の「構造化せよ・文字列にするな」ルールを公開サーフェスへ昇格）。基準B: *損失を伴う畳み込みは集計レイヤーに置き、プリミティブストリームには決して置かない* ── `check --format json`はサイトごとに忠実な`receiver_type`／`method_name`を運ぶ（`Constant<"x">`は`"x"`としてレンダリングされる）一方、`triage`が統計を意味あるものにする正規化を所有する。WD1は`Diagnostic#to_h`の2つの構造化フィールドを露出させる（nilのときは省略）。WD2は`triage`に`selectors`軸を加える ── `(receiver, method)`をキーとする`{receiver, method, count, files, rules}`、上限なしのJSON／テキストは上限付き、`--selectors-only`。WD3はリテラル畳み込みを`Triage.normalize_receiver`に置く（ヒューリスティックな`Catalogue`と共有；`"x".nope`と`name.nope`の断片化を防ぐ）。WD4はディスパッチ対象を持つ11個のcall/defルールのフィールドを埋める（`flow.*`／ivarは対象を持たないため除外）。ガードレール: この軸は注意をルーティングする（`pre_eval:`／RBSオーバーレイ）だけで、深刻度には決して流し込まない（`feedback_false_positive_discipline`）。ADR-50 WD1のもとで凍結された公開語彙であり、ADR-60の凍結前の窓で着地した。2つのオンボーディングスキルが`jq`ワークフローを教える |
| ADR-62 | [アナライザーのミューテーションテスト（偽陰性／歯の計測）](62-mutation-testing-teeth-measurement/) | Accepted — ハーネスと最初の修正は2026-06-13に着地。方法論と裁定の規律は有効。残りのバックログは需要ゲートされている。開発専用のミューテーションハーネス（`tool/mutation/`）は解析対象コードを型から見える形で壊し、*生き残った*ミュータントを偽陰性の候補として読み取る — Rigorの反偽陽性の規律の双対であり、「歯を検証済み」（ADR-43／24）が計測可能になる。基準A: *生存者がシグナルになるのはRigorが具体的な型を保持している箇所に限る* — ほとんどのミューテーションは型不変な等価ミュータントでその生存は正しいので、素のキルレートではなく型認識アンカーフィルタが荷重を担い、ハーネスはエンジン自身の`type_of`を問い合わせるためインプロセスである。基準B: *仮定せず、裁定せよ* — 生存者はバグではなく候補であり、一部は意図的な沈黙で、修正が着地するのは健全性が完全で`make verify`がクリーンに保たれる箇所だけ（ADR-57のプロトコル）。WD1はPrismネイティブで自前に作る、`mbj/mutant`は却下（インプロセスの`type_of`が必要;パーサ／プロセスモデル／演算子の不一致）。WD2はウォームループ（`ProjectContext`の環境＋スキャンを一度＋`prebuilt:`でバイパスしたラン結果キャッシュ → 約6〜12 ms/ミュータント）。WD3はバックログとしてのスイープ（生存者を`(operator, receiver type)`でクラスタリングし、エージェント向けに`--json` → 自己改善ループ）。WD4は開発専用、ADR-50の凍結サーフェスの外。WD5の着地済みの修正 — FP安全なユニオンレシーバーの未定義メソッドの歯（すべての非nilアームが確実にメソッドを欠くとき発火）＋RBSクラスエイリアスの解決（`Mutex = Thread::Mutex`がメソッドサーフェスを持つようになった）＋広域ファズモード（`mutate.rb fuzz` — クラッシュ／ハング／非決定性;`lib/rigor`の2,706ミュータントがクリーン）。検討＋却下: nilableユニオンの歯（13プロジェクトのコーパス調査 → 発火約0＋偽陽性、N3の沈黙を維持;それでも調査は2つの偽陽性ガードを出荷済みのスライス1へ収穫した — 汎用メタクラス＋異クラス、mailの外部偽陽性を修正）。先送り: `argument-type-mismatch`の裁定、`Type::*`のセルフドッグフードRBS、ユーザー向け型保護カバレッジ。根拠: `docs/notes/20260613-mutation-teeth-harness.md` |
| ADR-63 | [ユーザー向け型保護カバレッジ](63-type-protection-coverage/) | Accepted — ティア1（静的プロキシ、`rigor coverage --protection`）とティア2（ミューテーション有効性、`rigor coverage --protection --mutation`）はともに2026-06-14に実装。`rigor coverage`を*保護*次元で拡張する — 「自分の型がどれだけ精密か」ではなく「もしバグを入れたら、Rigorはそれを捕まえるか」（ADR-62の歯の作業をユーザー向けに表面化したもの）。枠付け基準（ADR-62の基準A）: 常に*有効性／どこに型を追加すべきか*であって、決して素のミューテーション生存ではない（それは動作中のコードを怯えさせる）。ティア1は1回のディスパッチサイトスキャンで、各呼び出しを**レシーバー**が具体的に型付けされるか（`concrete_class_name`が非nil＝Rigorが噛みつ*ける*）対`Dynamic`かで分類する — 保護に対する健全な上界で、対話的／CIで走らせられるほど安価、保護されていない「ここに型を追加せよ」のサイトを列挙する;`PrecisionScanner`を再利用する。ティア2は真実のティアで、サイトごとのミューテーションキルレート（ADR-62）、オプトインの`--protection --mutation`、変更ファイルスコープであり、ハーネスの**狭い**部分集合（ファイルごとの有効性 — 開発スイープ／ファズ／クラスタリングはADR-62 WD4どおり開発専用にとどまる）だけをプロダクト化する;撤回ではなくスコープされた精緻化である。WD1はコマンドを追加せず`coverage`を拡張する（`--threshold`ゲート＋`--format json`を再利用＝レポート＋ゲート）。WD2は保護＝`concrete_class_name(receiver)`が非nil、暗黙selfは除外。WD3はランク付けされた「ここに型を追加せよ」リスト＋ADR-61のJSON。WD4はティア2を段階化＋変更ファイルをデフォルトに。却下: 素の生存率を見出しにする、`rigor teeth`コマンド、ミューテーションをデフォルトティアにする、ハーネス全体を出荷する。根拠: ADR-62＋`docs/notes/20260613-mutation-teeth-harness.md` |
| ADR-64 | [非nilの引数型不一致とcoerceの障壁](64-non-nil-argument-type-mismatch/) | Accepted — 非nilチャネルは**多重オーバーロード**メソッドに対して構築・ゲートされた。本サイクルはまず`call.argument-type-mismatch`のFP安全な`nil`チャネルを出荷した（すべてのオーバーロードに拒否される純粋な`nil`引数で、nil受理をRBSパラメータ型で判定するので多重オーバーロード＋インターフェイスエイリアスパラメータを見通す;`param_admits_nil?`、コミット`a2775c7b`／`0afc65ea`）。非nil（`type_swap`）の誤った型の引数クラスタ（約900ミュータント、ADR-62スイープで残存最大の偽陰性）はRubyの**`coerce`プロトコル**に阻まれている: `5 + Money.new`はRBSのどの`Integer#+`オーバーロードも`Money`を列挙しないにもかかわらず`Money#coerce`を介して有効なので、素朴な「どのオーバーロードも引数を受け付けない」ルールはあらゆる型強制可能なユーザー型で偽陽性を出す。基準＝**ランタイムの受理が型だけから決定可能**な箇所でのみ発火する: （WD1）**coerceディスパッチ演算子を除外する**（`+ - * / < > …`、`UNIVERSAL_EQUALITY_METHODS`のような固定の許可リスト）、そこでは`coerce`が常に救える;（WD2）残りの非演算子メソッドについては`param_admits_nil?`を`param_accepts_arg_class?`へ一般化する（引数のクラスはパラメータの`_ToStr`／`_ToInt`インターフェイスを実装するか、`expand_type_alias`＋`interface_method_names`を再利用）;（WD3）単一の具体的なRBS既知の引数クラスに限る。ゲート（コーパスが新規コーパス偽陽性ゼロで本物の歯を示すときだけ構築する）は**達成**: 31の`rigor-survey`プロジェクト（Mastodonの`app`＋`lib`を含む）にわたる新規発火ゼロ;`type_swap`ミューテーションのキル数が上昇（kramdown 5→9、liquid 0→7）。却下: 素朴な全オーバーロード拒否（coerceの偽陽性）;`coerce`のモデル化（有界でない）。持ち越し: 名前空間化された型エイリアス解決（`::MatchData::capture`）;単一オーバーロードの比較演算子パスは不変。根拠: ADR-62＋歯ノート |
| ADR-65 | [診断の証拠ティアとドキュメントURL](65-diagnostic-evidence-tier-and-doc-url/) | Accepted — 2026-06-15に実装。精度に対して加算的（新しい診断はなく、バイト単位で同一）。公開診断サーフェスに追加した2つのフィールドで、2026-06-15の型ツール比較開発者フィードバックレポート（§4＋§5.1）に由来する。`evidence_tier`（`high` / `medium` / `low`、情報提供用ヘルパーには付かない）＝発火が真陽性であるというRigor自身の確信度と、安定した`documentation_url`。[ADR-61](61-agent-friendly-diagnostic-statistics/)（構造化ストリームフィールド）の後続で、*確信度*軸と*参照*軸を追加する。WD1: ティアは`RuleCatalog`の**ルールごと**の属性であり、発火ごとの計算ではない ── Rigorの偽陽性規律は不確実なケースをルール発火の*前*にフィルタするので、残った確信度はそのルールのゲートに支配される（フィードバック自身の§3も発火ごとではなくルール種別ごとに推論している）。WD2: ティアのセマンティクス ── `high`＝具体的な静的型でメタプログラミングの抜け道なし、`medium`＝ドキュメント化された偽陽性の許容範囲を持つフロー／推論の証明、`low`＝レビューへ送る解決／カバレッジギャップのシグナル（`call.unresolved-toplevel` → `pre_eval:`）── **深刻度に直交し、ゲーティングには決して供給されない**（偽陽性規律のガードレール）。WD3: `documentation_url`は公開カタログ内のルールごとのアンカー（`04-diagnostics.md#rule-<id>`）で、gemspecの`documentation_uri`方式を踏襲する。`rigor.dev/rules`サイトはでっち上げない（存在しないページへのURLは何もないより悪い ── 誠実さの精神）。設定可能なベースは先送り。WD4: `check --format json`では診断ごとに出力（組み込みルールのみ。ルールIDからCLIのJSON経路で補強されるので構築サイトは変わらない。`evidence_tier`はnilのとき省く）、`rigor explain` / `--format json`ではルールごとに出力（これにより§5.1が望んだドキュメントURL付きの機械可読な分類体系に無料でなる）。フィードバックの入れ子の`evidence`オブジェクトではなく**フラットなフィールド**（rationaleは`rigor explain`にあり、`fp_suppression_considered`は普遍的に真）。同じ変更でカタログの欠落（`call.unresolved-toplevel`に`RuleCatalog`エントリーがなかった）も塞ぎ、網羅性スペックを追加する。却下: 発火ごとの動的ティア、入れ子オブジェクト、存在しないページへのドキュメントサイト、ティアを深刻度／終了コードへ供給すること。[ADR-50](50-release-engineering-and-stability-strategy/) WD1のもとで凍結された公開語彙 |
| ADR-66 | [タグ付きユニオンのメンバー型付け（タグキーによるナローイング）](66-discriminated-union-member-typing/) | **Proposed — 未実装。需要ゲート付き**。多相なメンバーフィールド（例: `kramdown::Element#value`）を、その兄弟である**判別タグ**フィールド（`Element#type`）でナローイングし、ペイロードの型がタグの関数になっているAST／ビジター／コンバーターコード ── ペイロードがタグの関数になっているパーサ／テンプレート／リンターのジャンル（`case node.type; when …; node.<payload>`）── が、ペイロードの読み取りを`Dynamic`に落とさず型付けできるようにする。Rigorはすでに両半分を所有していながらつないでいない。コンシューマー側は`==` / `case`/`when`でタグをナローイングし（[ADR-47](47-narrowing-driven-clause-reachability/)）、プロデューサー側はインスタンスごとのメンバーレイアウトを持つ（[ADR-48](48-data-struct-value-folding/)の`DataInstance`＋`data_member_layouts`）。欠けている接続詞が、`(tag ⇒ payload)`のマップと、判別子がナローイングされたときにペイロードをナローイングするルールである。基準（偽陽性の許容範囲。交渉の余地なし）: マップが健全な過大近似であるときに限りナローイングし、そうでなければフラットな／`untyped`型を保ち何もしない（no-op）── この機能は保護を*追加する*だけで、動作するコードの沈黙を診断へ変えることは決してない（[ADR-58](58-ivar-field-typing/)との縁戚関係）。段階化: WD1は加算のみの下限、WD2はTier 1の宣言された`%a{rigor:v1:variant(…)}`マップ → タグインデックス付きメンバーキャリア（両側記述＝偽陽性に安全で、明示的な`case`イディオムを助ける）、WD3はTier 2の推論されたマップ（予算ゲート付き。動的な構築のもとでは健全でない ⇒ きれいな過大近似のみを採用）、WD4はTier 3のビジターディスパッチのタグ束縛（[ADR-16](16-macro-expansion/)基盤による`send("convert_#{el.type}", el)`）── 最難だが実コンシューマーの大半がここに生きており、この非対称性が本件をADR-58より下位にランクづける。WD5はミューテーションがナローイングを無効化する。ADR-58が着地した／明示的な`case`が一般的だとコーパスが示した／ADR-63がこのホールを頻繁に表面化させたら再評価する。却下: 発火するフラットなユニオン、ガードなしのプログラム全体推論、汎用のGADTシステム。根拠: 2026-06-16の保護引き上げパイロット |
| ADR-67 | [パラメータ型推論（M3フロンティア）: 呼び出しサイトと本体内、精度加算的のみ](67-parameter-type-inference/) | **Accepted — WD1+WD3+WD5（呼び出しサイトのパラメータ推論 + 上限付き不動点）を実装;`coverage --protection`のみに配線（v0.2.8、`check`ウォークではない——診断はバイト同一）。WD2（本体内の構造的推論）はスパイクされ需要ゲート付き**。アルゴリズムコーパス調査の「M3 — 型なしパラメータ→メソッド全体がDynamic: **EXCUSED、追求しない**」という判定を、**保護カバレッジ**の根拠にもとづいて再検討する。メソッド／コンストラクタのパラメータは既定で`untyped`（漸進的（gradual）なエントリポイント。ブロックパラメータは推論されるが、`def`パラメータは呼び出しサイトからも使用からも推論されない）になるので、`def initialize(line); @line = line`は`@line`を`Dynamic`にし、あらゆる`@line.<method>`を未保護にする──これは[ADR-58](58-ivar-field-typing/)とは異なる。ADR-58のインスタンス変数型付けは具体的書き込みフィールドについては*実現済み*（`@line = Line.new`は`Line`を読み取る）だが、**パラメータ由来**のインスタンス変数には手を出せない。調査がM3をexcusedしたのはリーフスクリプトに呼び出しサイトの種がないからだが、2026-06-16のパイロットは、これが実アプリ（faradayの`Connection`/`Options`、hamlの`compile(node)` / `@template = template`）に**残る最大の保護の穴**であり、そこには呼び出しサイトが存在することを示した──だから保護がそれを再開させる。基準（パラメータを寛容に保つ[ADR-5](5-robustness-principle/)との整合）: 推論されたパラメータ型は**精度加算的であるに限る**──それが供給するインスタンス変数／レシーバー／保護／畳み込みを鋭くするが、呼び出し元の引数を拒否するためには**決して**参照されない（パラメータ境界は寛容なまま。RBSで宣言されたパラメータが常に優先する）。段階的: WD1は精度加算的に限る契約（ADR-58 WD1のビットを再利用する推論であって宣言ではない由来。境界診断を決して発火できない）；WD2は本体内使用の**構造的下界**（`arr.delete_at`⇒応答可能の下界。呼び出しサイト不要──excusedされたリーフスクリプトにも効く）；WD3は**呼び出しサイトユニオン**（TypeProf流、アプリ規模のてこ。全プログラム的→予算ゲート付き、[ADR-46](46-incremental-dependency-graph/)と緊張関係；未観測の呼び出しサイト／動的ディスパッチ→`untyped`へフォールバック）；WD4は健全性のためのフォールバック；WD5は予算＋停止性（[ADR-41](41-inference-budget-design/)のもとで上限を設けたワークリスト不動点、[ADR-57](57-self-call-return-adoption/)のメモを再利用）。却下: 推論を宣言済みとして扱う（ADR-5違反）；調査の「追求しない」（保護レンズについては置き換え）；TypeProf完全版の全プログラム解釈器を既定にする。根拠: アルゴリズムコーパス調査＋2026-06-16のパイロット＋TypeProf内部の先行事例ノート |
| ADR-68 | [プラグインが宣言可能なクラスビルダー畳み込み（Struct / Dataを超えるメンバーシェイプキャリア）](68-class-builder-folding/) | **Proposed — 需要ゲート付き（demand-gated）**。[ADR-48](48-data-struct-value-folding/)のメンバーシェイプキャリア基盤（`StructClass`/`DataClass` + `data_member_layouts`）を、2つのハードコードされたビルダー（`Struct.new` / `Data.define`）から、Struct風クラスビルダーの**宣言された**ファミリーへ一般化する。これにより`ConnectionOptions = Options.new(:a, :b, …)`（faradayのカスタムビルダー）が、`Dynamic`ではなく、その定数に結びついた名前付きメンバーシェイプクラスとして型付けされる。2026-06-16のパイロットのfaraday上限（約31%）は、`Options.new`で構築された定数（`Env` / `*Options`）が、RigorがRBS名前空間へ接続できない動的クラスオブジェクトとして型付けされることに縛られている──これはADR-48がすでに解決した同じメンバーシェイプの形であり、コアでないビルダーを*認識する*ところだけで塞がっている。基準: **宣言された**Struct風ビルダーから代入された定数（あるいはサブクラス／ローカル変数）は、メンバー＝ビルダー呼び出しのシンボル引数を持つADR-48のキャリアへ畳み込まれる；ビルダーは宣言されている（プラグインの寄与／組み込みのアローリスト）かStruct祖先まで辿れるものでなければならず、任意の`X = Y.new`から推測してはならない（クラスを返さない`.new`を誤って畳み込んではならない）；精度加算的に限る（認識されないビルダー→今日どおり`Dynamic`、ADR-48の偽陽性なしの契約）。WD1はキャリアと層を再利用し認識だけを足す；WD2は[ADR-16](16-macro-expansion/)基盤を介したプラグイン宣言のビルダー（`rigor-faraday`の`Options.new`が最初の利用者）；WD3はビルダー定義を辿る推論的認識（延期──`.new`はオーバーライドされてメンバー以外の値を返しうる）；WD4は[ADR-26](26-activerecord-relation-typing/)の`open_receivers`とビルダーの動的メソッドサーフェスについて合成し（畳み込まれたクラスは既知だがオープン）、再導出しない；WD5は精度加算的／誤認識ガード。ADR-26のARケース（*非有界な*メソッドサーフェス──オープンレシーバー──であって固定のメンバーレイアウトではない）とは別物。却下: 新しいキャリアを設ける；任意の`X = Y.new`を推測する；ARケースをここで畳み込む。根拠: 2026-06-16の保護向上パイロット |
| ADR-69 | [プラグイン可能なミューテーション基盤（キルオラクル + オペレータシーム）](69-pluggable-mutation-substrate/) | **Accepted — 両シームとも2026-06-17に実装、ADR-70と同時着地**。シーム1（キルオラクル）は`DiagnosticOracle`を抽出し、`TestSuiteOracle`がその隣に座れるようにした；シーム2（サイトセレクタ、`--include-dynamic`として表面化）は、融合オーバーレイが`Dynamic`サイトに対して盲目だと検証で判明したとき同日ADR-71から前倒しされた──`Mutator#dispatch_site_mutations`はあらゆるディスパッチサイト（`Dynamic`レシーバー含む）をミューテーションし、`--with-tests`にゲートされる（`Dynamic`サイトの唯一の保護はテストである）。[ADR-62](62-mutation-testing-teeth-measurement/) / [ADR-63](63-type-protection-coverage/)のミューテーション機構を一般化し、**キルオラクル**と**サイトセレクタ**を、`Protection::MutationScanner`に焼き込まれた前提ではなく注入されるコラボレータにする。今日では唯一のキルが「新しいRigor診断が出た」（`mutation_scanner.rb`の`classify`）、唯一のセレクタが「Rigorが噛みつけるサイトを残す」（`mutator.rb`の`filter_by_type`）であり；どちらもアナライザーの歯の用途には正しいが、テストスイートのコンシューマーには**誤り**である──そのコンシューマーは*テストを実行する*ことでキルし、**逆**の選択（テストが唯一の網となる`Dynamic`サイトをミューテーションする）を望む。基準（再利用可能なルール）: `Mutator`が知るのは*ソースをスプライスする*方法と*契約がどこにあるか*（アンカー）だけ；*何をキルとみなすか*と*どのサイトがミューテーションする価値があるか*はコンシューマーに属する──基盤がオラクル／セレクタを差し替えても表現できないケイパビリティは、シームの隙間であって、ミューテータをコピーする理由ではない。シーム1＝`DiagnosticOracle`（今日とバイト同一）を抽出し、[ADR-70](70-fused-protection-coverage/)が実装する`TestSuiteOracle`インターフェースを定義；シーム2＝型認識フィルタを1つの`BiteableSites`戦略にし、コンシューマーが`AllSites`を渡せるよう分離する。新しいユーザーサーフェスはなし（ADR-50のフリーズはCLI/JSON語彙であって、`Protection::*`クラスのシェイプではない）。却下: コンシューマーごとにミューテータをコピーする（保守上の罠）；両軸を1つにしたオラクル（ADR-70の漸進的短絡を台無しにする）；公開プラグインAPIを今すぐ出す（ADR-71に先送り）。ADR-70の最初の動的コンシューマーと同時着地させ、インターフェースを机上ではなく実地で動かす。根拠: 2026-06-17の戦略ノート |
| ADR-70 | [静的∪動的の融合保護カバレッジ](70-fused-protection-coverage/) | **Accepted — 2026-06-17に実装（`coverage --protection --mutation --with-tests`）、ADR-69と同時着地、faraday / liquid / mailで検証済み**。オプションの**動的オーバーレイ**を`coverage --protection --mutation`に追加する。*型*チェッカーがキルできなかったミュータントごとに、*テスト*がそれをキルするか問い（ADR-69の`TestSuiteOracle`、ランナーフック`--test-command`、bundler環境を剥がして実行）、各サイトを**両軸**で分類する──型保護された／テスト保護された／**無保護**（概念上の*二重保護された*バケットは型保護に畳まれる。型でキルされたミュータントはスイートに到達しない──漸進的短絡）。`--include-dynamic`（ADR-69のシーム2）は`Dynamic`レシーバーのサイトへ拡張し、マップを完成させる（liquidの`lexer.rb`は76→115サイト、テスト保護38＋本物のギャップ2）。成果物はこの融合である。型もテストもそのラインを守っていない**ときに限り**真に無保護であり、レポートは**より安価な欠落軸**（「型を足す」対「テストを足す」）を指摘する。静的な型保護と動的なテスト保護を1枚のマップに融合する既存ツールは存在しない。基準（[ADR-63](63-type-protection-coverage/)の枠組みを拡張）: 常に有効性／どこに保護を足すべきかであって、生の生存数では決してない──そしてペイロードは**帰属**（最も安価な欠落軸）であって単一の数値ではない。サイトが無保護なのは両軸とも外れたときに限る。漸進的短絡: 型でキルされたミュータントはスイートに到達しない（型 = 第1の防衛線、テスト = 第2）ので、コストは保護穴に比例する。WD1は生存者スコープ、オプトイン、変更ファイルのデフォルト;WD2はテストオラクルが**裁定された候補であって評決ではない**（[ADR-59](59-spec-assertions-are-not-signatures/): pending／タグ除外／flakyなテストは知りえない──生存者は注意を優先付けするのであって、ギャップを認証しない）;WD3はADR-69のシームを消費し、`DiagnosticOracle`パスはバイト同一;WD4は新しいフラグ＋JSONキーをADR-50の下で凍結する。**選択はスイートの仕事であって、依存グラフの仕事では決してない**（型読み取りグラフ≠ランタイムコールグラフ→キルするテストを飛ばす→**偽生存者** = FPであり、`feedback_false_positive_discipline`のラインは越えない）。却下: すべてのミュータントに対してスイートを走らせる（短絡なし）;単一の融合した%;グラフベースのテスト選択;新しい`rigor teeth`コマンド;テスト生存者を証明済みのギャップとして扱う。根拠: 2026-06-17の戦略ノート（判断ステップ2） |
| ADR-71 | [型ガイド付き外部インクリメンタルミューテーションテスト](71-type-guided-external-mutation-testing/) | **Proposed — 先送り／需要ゲート付き、未実装**。Rigorのミューテーション基盤を、RSpec / minitestを駆動し、Rigorの型＋依存グラフでミューテーション空間を枝刈りする**外部**ライブラリへ成長させるべきかを評価する。結論: **先送り**──目玉の売り込み（「型がミューテーション空間を枝刈りする→通常のミューテーションテストより賢く／速い」）は**過大評価されている**。生き残った真に新規なウェッジは、賢い枝刈りではなく、[ADR-46](46-incremental-dependency-graph/)の上に築く**インクリメンタル／CIスコープ**のミューテーションテストである。これは独立した*第三の*プロダクト──**ユーザーのテストスイート**のミューテーションテスト（生存者 = テストギャップ）であり、ADR-62の解析器の牙ツールとはPrismスプライサ（約20%）のみを共有する。テストランナーの80%は`mutant`の墓場だ。基準（各レバーは健全な箇所でのみ）: **ランタイムカバレッジ**がどのテストを実行するかを選択する（静的グラフは過大近似し、さらに動的／メタプログラムされた呼び出しを*見落とす*→キルするテストがスキップされる→**偽生存者** = FP）;**依存グラフ**がスコープするのは*差分でどのファイルをミューテートするか*だけ;**型情報**は生成＋層別レポートを担い、テスト選択は**決して**担わない。再評価トリガー（両方）: （a）ADR-46のインクリメンタルが着地済み（CIウェッジが必要とする差分クロージャエンジン）;（b）需要が実証される（ADR-70の動的軸が実プロジェクトで価値を示す、あるいはユーザーが要望する）。却下: 静的グラフのテスト選択（カバレッジに負け、偽生存者を生む）;「型による枝刈りで価値が出る」（価値の低い型付きサイトで噛みつき、安価な軸を枝刈りする）;今構築する（フリーズ前、80%の墓場）;エンジンとして`mutant`／`mutest`を採用する（構築時に再検討）;スタンドアロンの型ガイド付き生成（ADR-69/70に畳み込み）。根拠: 2026-06-17の戦略ノート（拡張的にも批判的にも展開した完全な分析） |
| ADR-72 | [Gemfile.lockでゲートされたバンドルRBSオーバーレイ](72-gemfile-lock-gated-rbs-overlays/) | **Accepted — 2026-06-17に実装**。プロジェクトの`Gemfile.lock`にgemがロックされているのに、どの解決経路を通じてもRBSを同梱していない（`RbsCoverageReport`の`:missing`クラス）とき、Rigorはそのコア型拡張向けにバンドルされたgem単位のRBSオーバーレイ（`data/gem_overlay/<gem>/`）を自動ロードする。これにより、Railsプロジェクトは`3.minutes` / `"x".underscore` / `hash.symbolize_keys`に対する系統的な偽の`call.undefined-method`を見なくなり、一方そのgemを持たないプロジェクトは依然として本物の診断を見る。ActiveSupportが最初の（v1ではこれのみ）オーバーレイであり、オプトインの[`rigor-activesupport-core-ext`](../manual/plugins/rigor-activesupport-core-ext/)プラグインの、自動適用されるロックゲート式の双子だ。根拠: 2026-06-17の`evidence_tier`校正フィードバック──`evidence_tier == "high"`（[ADR-65](65-diagnostic-evidence-tier-and-doc-url/)）を信頼する下流のコンシューマーが、Mastodon上の高ティアの`call.undefined-method`512件中171件（約33%、すべてActiveSupportのcore-ext）を「生きたバグ」へと自動昇格させた。基準: 偽陽性をソースで解決し、決してラベルを貼り替えない──プロジェクトの一次的な価値は偽陽性の規律であり、格下げはそれに寄与しない（ティアはADR-65 WD2により重要度に流し込まれないので、赤いエラーが動作するコードに残る）。WD1のゲートはロック済み∧`:missing`∧競合するオプトインプラグインがロードされていない（`GEM_OVERLAY_PLUGIN_IDS`）であり、`bundler.auto_detect`（デフォルトtrue）に便乗する;実際のgemの存在でゲートすることがこれを健全にする（素のRubyプロジェクトは本物の`3.minutes`の欠落を保つ）。WD2は偽陽性に安全な方向のみ──オーバーレイは診断を*取り除く*ことしかできず、しかもメソッドがランタイムで実在する箇所でのみ行う;本物のタイポ（`5.minuets`）は依然として`:high`で発火する（格下げ式の代替案ができなかった弁別）。WD3ではオーバーレイのディレクトリが`for_project`の`loader_signature_paths`に乗るので、環境キャッシュ記述子がそれをダイジェストし、ロックファイルの変更で無効化される──新しいキャッシュ配管は不要。WD4は**ADR-65 WD1を保持し**（ティアはルール単位のまま;発火単位の動的ティアなし）、**ADR-27/31のプラグイン自動ロードの先送りを覆さない**（あちらはプラグインの*コード*を先送りする;こちらはRBSの*データ*のみをロードし、存在でゲートする──`core_overlay`の仕組みをロックファイル条件付きにしたもの）。WD5の`GEM_OVERLAY_PLUGIN_IDS`＋`data/gem_overlay/`ツリーは、他の系統的なコア型モンキーパッチャ（i18n、Sequel）へ、ディレクトリを追加することで一般化する。需要ゲート式だ。却下: 発火単位のティア格下げ（フィードバックの文字どおりの要望──ADR-65の却下された発火単位の動的ティアを覆し、赤い偽陽性を動作するコードに残す）;モンキーパッチのリスク下での重要度の格下げ／抑制（本物のコア型タイポへの偽陰性と引き換えになる）;完全なプラグインの自動ロード（コードを実行する、ADR-27/31）;無条件のオーバーレイ（Rails以外のプロジェクトで健全でない）。根拠: 2026-06-17の`evidence_tier`校正フィードバック |
| ADR-73 | [SKILL駆動のRigorユーザー体験](73-skill-driven-user-experience/) | **Accepted — WD1〜WD5を2026-06-20に実装;`describe`カタログは成長するよう設計されている（最初の追加は`rigor-rbs-setup`、同日に着地）**。「次に何をするか」のための単一のSKILL駆動エントリーポイント（`rigor-next-steps`）を確立し、それをライブでバージョン同期した`rigor skill --describe`が支えることで、配布されるオンボーディングのガイダンスが決して陳腐化しないようにする。基準: あるガイダンスが配布されるSKILLへ凍結されるのは**`rigor`が存在する前に実行されねばならない場合に限る**（コマンドの解決、インストール）;Rigorの進化とともに変化するものはすべて——下流スキルのカタログ、説明、プロジェクト状態→次ステップの推奨——`rigor skill --describe`によって**ライブで**出力され、SKILLへコピーされることはない。WD1の`rigor-next-steps`の本文＝ブートストラップ連鎖のみ（`rigor`を解決→`docs/install.md`経由でインストール→未設定なら`project-init`→さもなくば`--describe`へ委譲）。WD2の`--describe`の契約＝安価な**存在のみ**のプロジェクト状態の探査（設定 / ベースライン / `sig/` / CI——`rigor check`は**決して**実行しない）＋推奨される次スキル＋フロントマター説明付きのカタログ＋エージェント向けアクションプロンプトで、`SkillCommand`（[`lib/rigor/cli/skill_command.rb`](https://github.com/rigortype/rigor/blob/master/lib/rigor/cli/skill_command.rb)）上で読み取り専用かつ副作用なし。WD3のインストール分岐は唯一`--describe`にできないステップ（まだバイナリがない）なので`docs/install.md`の生URLを指し、`install.md`のステップ4は同じルーティングへ折り返して委譲するので両者は食い違えない。WD4は保留されパイロット検証済みの`rigor-protection-uplift`（[ADR-63](63-type-protection-coverage/) WD5、5つのOSSリポジトリで+4.7pp／リグレッションゼロ）を`skills/`へ昇格させ、そのパッケージングを同じ二重チャネルとして確定する。WD5の配布の衛生＝2つのチャネル・1つのソース（vercel-labs/skillsの`npx skills add`によるインストール前＋gem同梱の`rigor skill`によるインストール後、両者とも同じ`skills/`ツリーを通じて、[ADR-31](31-contribution-and-supply-chain-policy/)に従い2つ目の成果物なし）＋`.claude/skills/`を`metadata.internal: true`とマークして一括インストールが貢献者ワークフローを出荷しないようにする＋新しい`skills/README.md`。却下: ルーティングを配布されるSKILLに埋め込む（これが防ぐ陳腐化）;`--describe`が完全な解析を実行する（存在のみのヒントには遅く副作用がある）;`rigor`サブコマンドのエントリーポイント（自身の不在をブートストラップできない）;別個の`rigor-skills` gem／リポジトリ（ソースを重複させる）;`.claude/skills/`を発見可能なままにする（リリースツールをユーザーへ出荷）。新しい`--describe`出力＋スキルidは、[ADR-50](50-release-engineering-and-stability-strategy/) WD1のもとv1.0で凍結される公開語彙になる。カタログは開かれている: 新しい機械的なdescribe経由のスキルはそれぞれ、gem側の決定木における安価な存在のみの分岐1つだ——最初の7つが同日に着地した: `rigor-rbs-setup`（`rbs collection install`、gem-RBSギャップでの見出し分岐）、カタログ優先のDX／統合ペア`rigor-editor-setup`（`rigor lsp`）＋`rigor-mcp-setup`（`rigor mcp`）はマニュアルのエディタ／MCP章へルーティング、そしてカタログのみのメンテナンス／検証スキル`rigor-monkeypatch-resolve`（`pre_eval:`）、`rigor-plugin-tune`（`rigor plugins --strict`）、`rigor-upgrade`（`rigor diff`／`baseline regenerate`）、`rigor-doctor`（`config_warnings`＋`plugins --strict`＋`baseline drift`、新しいコマンドなし）。ジャーニー分岐とカタログ優先の区別が規則だ: スキルが見出し推奨を獲得するのは、信頼できる存在シグナルを露出するときに限る。フィールドトライアルのフォローアップ（2026-06-20のconference-app＋rigor-surveyドッグフード、`docs/notes/20260620-skill-driven-onboarding-dogfood.md`）: 探査は7/7で正確だった;着地したUX修正（target_ruby下限メッセージ、実行可能なメタgemエラー、欠落パスの警告＆スキップ、`rigor describe`エイリアス、**WD2を保持するcheck対応のエージェントプロンプトルーティング**）;批准のために先送りされた未決定事項——見出しのcheck対応（`.rigor/`キャッシュ読み取りvs `describe --deep`オプトイン、`check`を実行するデフォルトには決してしない）＋`rbs-setup`の優先度の緩和。根拠: 保留されたact-on-coverageスケルトン＋`docs/install.md`＋既存の`rigor skill`コマンド |
| ADR-74 | [オフラインドキュメントアクセス（`rigor docs`）と`llms.txt`連携](74-offline-doc-access-and-llms-txt/) | **Accepted — WD1〜WD4を実装（`rigor docs`、v0.2.2）**。[ADR-73](73-skill-driven-user-experience/)をドキュメント軸で完成させる: インストール済みのRigorが、エージェントの必要とするドキュメントを**オフラインで**提供し（gem同梱の`docs/manual`上の新しい`rigor docs`コマンド経由）、プロジェクトの`llms.txt`エージェント向けドキュメント発見インデックスが新しいスキルサーフェスを反映しつつオフラインパスを記録する。基準: エージェントが*Rigorを駆動する*ために参照するドキュメントはgemに属し、`rigor docs`が提供する（スキルにおける`rigor skill print`を鏡映する）——こうしてRigorがインストールされれば、SKILL駆動のUXはgemがすでに携えているガイダンスのためにネットワークを必要としない;Web上の`llms.txt`＋GitHubの生URLは**インストール前**のフォールバックにとどまる（Rigorのインストールはローカルの`rigor`に必ず先行する）。WD1はgemspecで`docs/manual/**/*.md`＋`docs/install.md`をバンドルする（Rigorを駆動する章;貢献者向けのADR／spec／notesコーパスはWebのみのまま）。WD2は`rigor docs`（インデックス／`<name>`章／`list`／`path`、`lib/rigor/cli/docs_command.rb`、`rigor skill`の形状を鏡映;バンドルされた`docs/`を読み、ネットワークなし）＋gem側の`llms.txt`ミラー。WD3はエディタ／mcp／next-stepsのスキルが`rigor docs <chapter>`（ローカル）を優先し、GitHub URLをインストール前のフォールバックとする（`next-steps`のインストールステップのみが既約的にネットワーク——`docs/install.md`は`rigor`に先行する）。WD4はサイトの`llms.md`／`llms-ja.md`を刷新する（「1つのプロンプト」を`rigor-next-steps`経由でルーティング、スキルリスト→カタログ＋`rigor skill describe`、索引の第17章、オフラインアクセスの注記を追加）——マニュアルが両方の`llms.txt`コピーが索引する単一のソースだ。却下: ドキュメントをWebのみに保つ（ネットワーク依存）;別個のドキュメントgem（ADR-31の単一gemを重複させる）;サイトの`llms.txt`をランタイムで取得する（ネットワークへ再結合する）;`docs/`全体をバンドルする（貢献者向けの肥大化）。`rigor docs`＋`llms.txt`の語彙は[ADR-50](50-release-engineering-and-stability-strategy/) WD1のもとv1.0で凍結される。根拠: ADR-73＋デプロイ済みの`llms.txt`＋フィールドトライアルノート |
| ADR-75 | [`Dynamic[T]`の来歴（provenance）と説明](75-dynamic-provenance/) | **Accepted — 2026-06-24に実装（`01e291cb`）**。`Dynamic[T]`は未検査の値に適したキャリアだが、エンジンはある値がなぜdynamicになったかを記録していなかった。これに対して精度加算的な**来歴**（provenance）のサイドチャネル（external-gem-without-rbs / framework-dsl-boundary / analyzer-budget-cutoff / explicit-untyped / unsupported-syntaxという固定の原因集合）を追加し、`coverage --protection`のラベル＋`--format json`を通じて加算的に表面化する。対処可能な穴（RBSをインストールする、プラグインを有効にする）と対処不能な穴（DSL境界）を、`untyped = Dynamic[top]`の意味論を変えず、診断を発火させず、深刻度にも供給せずに区別できるようにする |
| ADR-76 | [`freeze` / `dup` / `clone`のエフェクトモデリングとシェイプキャリアの保存](76-effect-modeling-freeze-dup-shape-preservation/) | **Accepted — WD1は2026-06-24に実装（`2751bc78`）;WD2は2026-06-26に完全着地（HashShape＋Tupleの保存、ADR-78 WD1のブロック形式過剰畳み込みガードが残存する反射的発火を取り除いた後）**。エフェクト／ミューテーションモデリングの分割決定。WD1（保守的無効化半分：非破壊呼び出しはファクトを保存し、エイリアシングを伴うミューテーション呼び出しは無効化する）は着地済み。WD2の高価値な「純粋self返却子を通じたシェイプキャリア保存」は別ブランチへ先送り（反射的な`flow.always-truthy-condition`をRigor自身の定数畳み込み器に12件発生させたため） |
| ADR-77 | [`rigor doctor`と`rigor upgrade`の証拠ルーティングコマンド](77-doctor-and-upgrade-commands/) | **Accepted — 2026-06-24に実装（`8048991c`;`upgrade`はスケルトン）**。デフォルトの`check`を変えるのではなく、既存の証拠を*ルーティングする*2つの加算的CLIコマンド。`rigor doctor`はすでに生成された所見（設定解決の警告、`RBS classes available: 0`、Rails固定だがRailsプラグインなし、ベースラインのドリフト）を、セットアップ問題か健全な実行かに分類しルーティングされた次のアクションを示す——新しい解析パスもルールも追加しない。`rigor upgrade`は[ADR-50](50-release-engineering-and-stability-strategy/) WD7の移行コマンド（具体的なBCが目標を与えるまで先送り） |
| ADR-78 | [反射的な過剰畳み込みと`flow.always-truthy-condition`の許容範囲](78-reflexive-overfold-always-truthy/) | **Accepted — 2026-06-26に完全実装**。WD2（`REFLECTIVE_SEND_METHODS`ガード）＋WD1の持ち越し（`ShapeDispatch.try_dispatch`のブロック形式過剰畳み込みガード）＋WD3（HashShape**および**Tupleのシェイプキャリア保存、lib＋mail/kramdownコーパスでmasterと診断同一）。`receiver.public_send(method_name)`のようなランタイム変数のメソッド名を持つ反射的ディスパッチが単一の`Type::Constant`へ過剰に畳み込まれ、ルールがそれを証明可能に真と読んでいた——Rigor自身の定数畳み込み器での12件の反射的発火が[ADR-76](76-effect-modeling-freeze-dup-shape-preservation/) WD2を差し戻させた原因 |
| ADR-79 | [チェッカー固定の決定性よりRBSバージョン範囲への忠実性](79-rbs-version-range-over-pinned-determinism/) | **Accepted 2026-06-26**。コア/標準ライブラリのRBSをRigor固定のバンドルにせず、プロジェクト自身が解決した`rbs`gem（gemspec範囲`rbs >= 3.0, < 5.0`）から読み続けるという決定を記録する。基準: **インストール済みツールチェーンへの忠実性が、環境をまたぐバイト決定性に優先する**——`rbs`バージョン間の診断の差異はバグではなく「実行環境どおりに検査している」ことであり、決定性はバージョンギャップが*誤った*結果を生む箇所でのみ*戦術的に*買う（RBSソースを凍結して構造的に買うのではない） |
| ADR-80 | [`type_specifier`プラグインフックを`narrowing_facts`へ改名](80-narrowing-facts-rename/) | **Accepted 2026-06-26;0.3.0で完了（COMPLETED）**。ADR-37スライス2のプラグイン作者向けDSL動詞`type_specifier`を`narrowing_facts`へ改名する;`type_specifier`は0.2.x系列を通じて警告を出す非推奨エイリアスとして生き残り、**0.3.0で削除**される;同梱のminitest/sorbet/rspecプラグインは移行済み。`type_specifier`は`dynamic_return`（*型*を返す）と対をなす名前に読めるが、実際には**戻り値後のナローイングファクト**を返す——内部ストレージの名前（`post_return_facts`）の方がすでに実態に即している。0.3.0でエイリアスが削除され、先送りしていた内部名も完全な一貫性のため一括改名された（`type_specifiers`→`narrowing_facts_rules`、`#type_specifier_facts`→`#narrowing_facts_for`、JSON `type_specifier_methods`→`narrowing_facts_methods`）——JSONキーはv1.0で公開語彙として凍結される（[ADR-50](50-release-engineering-and-stability-strategy/) WD1）ため、破壊を許すマイナーが誤った名前を直せる最後の無料ウィンドウだった |
| ADR-81 | [スキルセット最適化: スキルごとの鮮度 + `waza`評価スタンス](81-skill-set-optimization/) | **Accepted — 2026-07-05実装**。ユーザー向けの`skills/`セットを維持するための2つの恒久的な決定。**基準1（鮮度）**: [ADR-73](73-skill-driven-user-experience/) WD1の「インストール前に必要なものだけ凍結する」ルールを、エントリポイントから*すべての*スキル本体へ一般化する——配布されるスキルはバージョン安定な骨組みだけを凍結し、バージョンに結び付いた手順の詳細はインストール済みgemからライブで取得する。要となる点: 両方の配布経路（gemの`rigor skill` + vercel-labsの`npx skills add`）は単一の`skills/<name>/SKILL.md`を読むため、鮮度はファイルを薄くすることからは得られない（それは両方を薄くしてしまう）——鮮度は、新設の`rigor skill --full <name>`（本体 + すべての`references/*.md`を1回の呼び出しでインライン展開する、`SkillCommand#run_full`）経由で現行の本体を再取得させる**「まず: バージョン最新のコピーを読み込む」ディレクティブ**から得られる;凍結されたベンダリング済みコピーはインストール時点で止まる一方`rigor skill`は常にgemを読むので、ディレクティブはアップグレード時の乖離をインストール済みバージョンの側へ解決する。ハイブリッドな調整: 本体がドリフトする正確なフラグ／設定／ルールIDを抱える箇所（`rigor-doctor`＝実演的な分割）でだけ揮発性の詳細を`references/`へ切り出し、それ以外はディレクティブのみ;`rigor-next-steps`は純粋なインストール前ブートストラップのままとする。**基準2（評価）**: `waza`の助言は、agentskills.ioの公開プロファイルから独立した欠陥を指摘する場合にのみ採用する——**採用**するのはリンクの健全性（`waza check`が`rigor-ci-setup`内で`blob/main`の404を4件検出、デフォルトブランチは`master`）とハードコードされたドキュメントURLの過剰な具体性（オフラインの`rigor docs`へ誘導、基準1と収束する）;**却下**するのは500トークンのバジェット／「complexity: comprehensive」（スキルは意図的に包括的である）と`**WORKFLOW SKILL**`／`USE FOR:`／`INVOKES:`のラベル付きdescription形式（散文の`Triggers:`／`NOT for … (use X)`が同じことを伝えつつ読みやすい;その欠如を`waza`が減点するのは公開バイアスである）。description長（クロスモデルの密度で60語以下）はスキルごとの判断であり、過度に広い受け皿（`rigor-ask`、169語）にのみ適用し、トリガー再現率を犠牲にする一律ルールにはしない;`waza dev --auto`は禁止のまま。`rigor skill --full`はv1.0で公開語彙として凍結される（[ADR-50](50-release-engineering-and-stability-strategy/) WD1）。見送り: エントリー以外のスキルのvercel-labs配布を取りやめること（ADR-73 WD5を覆す）;`rigor-ci-setup`のインラインテンプレートを`rigor docs ci`へ委譲すること（パリティ検証済みのリファクタリング）。メカニズムの詳細: ADR-73の2026-07-05修正。根拠: 本セッション;`CLAUDE.md` § "Evaluating skills with `waza`";[ADR-74](74-offline-doc-access-and-llms-txt/) |
| ADR-82 | [`Dynamic[T]`の由来（provenance）配線: 実アプリでキャッチオールを打破する](82-dynamic-provenance-wiring/) | **WD1+WD2+WD3+WD6+WD7+WD8を2026-07-06実装;WD9（外部gem定数の由来 → `external_gem_without_rbs`/`add_rbs`）を2026-07-11実装;アクション可能性の弧はほぼ使い尽くした**。[ADR-75](75-dynamic-provenance/)は`Dynamic[T]`の由来サイドチャネルとその`coverage --protection`のtractabilityラベルを追加したが、Mastodonのフィールド計測（app+lib、未保護サイト21,119件）はそのラベルが実際のRailsアプリでは情報量に乏しいことを示す: **84%はキャッチオールの`unsupported_syntax`（→ `engine_gap`）に落ち、14%は原因を持たない**一方、ユーザーがアクション可能な2つの原因——`framework_dsl_boundary`と`external_gem_without_rbs`——は**ゼロ**サイトでしか発火しないので、ありふれたイディオム（`Tag.find_normalized(...).id`、`current_user&.account`、`params[:limit].present?`）がすべて扱い不能と誤ラベルされる。原因は2つのエンジンギャップだ: **G1**ルックアップがレシーバーノードローカルである（`ProtectionScanner`はディスパッチの直近のレシーバーノードで由来を読むが、`MethodDispatcher`はDynamicを*生み出した*呼び出しノードで原因を記録する——ローカル/ivar読み取りのレシーバーは決して一致しない）、そして**G2**特定の原因はまれにしか成立しない条件下でのみ記録される（`framework_dsl_boundary`はプラグインがDynamicを返すときだけだが、プラグインは具体的な型を返す＝保護済み;`external_gem_without_rbs`はオプトインのADR-10/`pre_eval:`を要する;`try_discovered_method`/`try_user_class_fallback`は何も記録しない）。基準＝帰属の誠実さ（健全な場合にのみ原因を記録/伝播する;疑わしいときは汎用的な原因を保つ——誤った「プラグインを有効化せよ」ヒントはユーザーの時間を浪費する）。WD1レシーバーノードのルックアップをまたいで由来を伝播する（ローカル/ivar読み取りのレシーバーをそのバインディングの起点まで辿る;フロー変動する`Scope`のサイド関連付け——難しく、見送られ、パフォーマンスゲート付きのスライス）· WD2ラベルのない2つのユーザーメソッド層で原因を記録する · WD3新しい`inferred_return_untyped`原因（`engine_gap`、[ADR-67](67-parameter-type-inference/)/[ADR-58](58-ivar-field-typing/)へルーティング）で「エンジンがこの戻り値を推論できない」がunsupported syntaxと誤ラベルされなくなる · WD4汎用的なフレームワーク境界の記録はプラグインごとのフォローアップへ見送り（推測は誠実さに反する）· WD5まずWD2+WD3を着地させ、再バケット化を計測し、その後WD1のコストを判断する。**WD2+WD3が着地・計測済み（2026-07-06）: 211サイト（約1%）を`inferred_return_untyped`へ再バケット化、すべて正しく帰属されたチェーンレシーバーのホール、比率は不変。次にWD1が着地（`Scope#local_origins`/`#ivar_origins`の名前→原因のサイドテーブルを代入時に設定し、`ProtectionScanner`が後追いする）: パフォーマンス中立（アロケーション+0.1%）だが控えめ（nullバケットのうち約322サイト）——残余を裁定すると、支配的なキャッチオールは素の変数ではなく中間の呼び出し/チェーンレシーバーだと分かり、次のレバーをWD6のチェーン起点継承へ方向転換した。次にWD6も着地（`Dynamic`レシーバーのディスパッチが、そのレシーバーの実効起点——共有の`Inference::OriginLookup`——を、それが生み出す呼び出しへ記録し、原因がチェーンを生き延びる）: パフォーマンス中立（アロケーション+0.03%）で、原因なしバケットを半分以上削る（MastodonのnullはWD1+WD6で2,921→1,356）。だが伝播された原因のほとんどは、チェーンの根がそれを記録するため`unsupported_syntax`だ。したがってWD6はtractabilityの*アクション可能性*よりも由来の*完全性*を買う——次のレバーは根を拡充することだ。次にWD7が2つの結合した変更を着地させた: （a）サイトごとの正確な`cause_site_counts`メトリクス（`tractability_summary`が今やそれから導出される）——これが**上記のWD1/WD6の数字を、グループ支配的なロッシーなアーティファクトとして訂正した**: WD1+WD2+WD3+WD6後の真の原因なしカウントは21,119件中10,390件（49%）であって1,356件ではなく、由来の完全性は約94%ではなく約51%だった;そして（b）最初の根の拡充——未宣言のパラメータの由来をメソッド入口で`inferred_return_untyped`にシードする（型なしパラメータは典型的なADR-67のギャップだ）。これをWD1/WD6がチェーンを通して伝播し、Mastodonの約3,100件のホールを原因なしからパラメータ推論へルーティング済みへ移す（真のアクション可能性の獲得;原因なし10,390→7,305）。次にWD8が、束縛されていないivar読み取り（フィールドが未追跡の`@x.foo` → ADR-58）に同じことを行い、さらに−1,900件。累積WD7+WD8: 原因なし10,390（49%）→5,405（26%）、アクション可能な`inferred_return_untyped`は351→5,399（15倍）、比率は不変;残余の原因なしは`dynamic_top`ノード種（yield/super/block）とクラス/グローバル変数の読み取り（ほとんどは本当に未モデル化）なので、アクション可能性のレバーはほぼ使い尽くした**。全体を通じて精度加算的（型/診断/重大度の変更なし、ADR-75のサイドチャネル契約）;ゲートはゼロデルタのコーパスではなく、再バケット化の計測 + 帰属の裁定だ。却下: `Dynamic`キャリアに由来を載せること（値の等価性が壊れる、ADR-75 WD1）、レポート時の再導出（層が誤り）、単一の「unknown」バケットへの集約（シグナルを放棄する）、由来の診断（偽陽性の規律）。根拠: [2026-07-06 Mastodonカバレッジ/由来ノート](../notes/20260706-mastodon-coverage-provenance-and-siggen-rbs-validity/) + [2026-07-04オンボーディングノート](../notes/20260704-rails-coverage-onboarding-carrier-trap/) §O5 |
| ADR-83 | [Dynamic-origin代数: `Dynamic`へ吸収せずユニオンアームを保つ](83-dynamic-origin-algebra/) | **Accepted 2026-07-11**。[`value-lattice.md`](../type-specification/value-lattice/)にある創設期の動的由来の**ジョイン**代数（`T \| Dynamic[U] = Dynamic[T \| U]`、具体的なユニオンアームを`Dynamic`へ吸収する）は**置き換えられた** —— `Type::Combinator`はそれを一度も実装しておらず、スパイク（3つの恒等式すべてを実装し、計測する）が**ユーザーに見える価値ゼロ**を見つけた: セルフチェックの`lib`診断は同一、`lib`のカバレッジ`precise_ratio`は同一（0.5632;dynamic_opaqueはむしろ*上昇*）、Mastodon `app/models`の診断は同一で、その一方で最もホットなパス（`union`/`intersection`）に`Dynamic`スキャン + 再帰を加える。基準: *値の結合規則がその地位を得るのは、精度または保護を計測可能に改善する場合に限る;表現を再正規化するだけの規則、またはどの消費者も読まないprovenanceのために精度を犠牲にする規則は採用しない*。ジョインは具体的なアームを`Dynamic`へ吸収する（精度に負、[ADR-82](82-dynamic-provenance-wiring/)/[ADR-67](67-parameter-type-inference/)の保護の弧に反する）——独立したユニオンアームを保つことは厳密により精密で、アームごとのprovenanceを携える。**ミート**規則（`Dynamic[T] & U = Dynamic[T & U]`）は唯一の精度に正なものだが、フローナローイングが既に*それ以上*を行う（`narrowing.rb:2317`はガードされた`Dynamic[top]`を`Nominal[String]`＝保護済みへ具体化する）;provenanceを保存する形式は需要ゲート付きのストリクト動的規律（[ADR-75](75-dynamic-provenance/) WD4）にとってのみ重要で、今日は保護をリグレッションさせるだろう。決定: エンジンのユニオン保存 + ナローイング具体化の振る舞いを規範として保ち、`value-lattice.md` / `normalization.md`を一致するよう改訂する。却下: 代数を実装する（価値ゼロ、精度に負、ホットパスのコスト）;「計画中」とマークする（それは保留ではなく意図的に望まれていない）。根拠: [2026-07-11のdynamic-facetスパイクノート](../notes/20260711-dynamic-facet-algebra-spike/) |
| ADR-84 | [クロスファイル戻り値メモのスコープ設定とtaint-preciseなストアゲート](84-cross-file-return-memo-scoping/) | **Accepted — WD1（プロファイルカウンタ + 記録の健全性ピン）を[PR #79](https://github.com/rigortype/rigor/pull/79)で着地;WD2（実行スコープのバケット + 依存関係のcache-and-replay）とWD3（イベントテイントのストアゲート）を実装**。呼び出し先本体の繰り返し評価は、3度目に表面化した構造的なコールドコストだ（[ADR-57](57-self-call-return-adoption/)で約+12%コールド;PR #62のbisectステップ）。PR #79のカウンタは、ADR-57フォローアップメモの信じられていた形を2点で訂正した: バケットはファイルごとにマージされた`discovered_def_nodes`のアイデンティティでキーイングされるのでヒットがファイル境界を決してまたがない（mailの`Mail::Part#has_content_type?`は**564回**評価された）、そしてunroll-in-flightの候補一律除外が、実際の一時的な影響に関係なくmailの本体評価の82%を拒否する。基準1 **finality** —— エントリーは`(def_node, receiver, arg_types)`と凍結された実行グローバルインデックスの決定論的な純関数だけを保持できる;一時性は、干渉したかもしれないだけの機構に対する一律の事前ゲートではなく、*計算をブラケットするイベントによって事後的に*検出される。基準2 **観測的等価性** —— ヒットは、あらゆる要となる消費者にとって新鮮な評価と区別不能でなければならない: 返される型*かつ*[ADR-46](46-incremental-dependency-graph/)の記録された依存エッジ、なのでクロスファイルのヒットはエントリーに付随した読み取り集合のリプレイをREQUIREする（メモを迂回しつつ記録する素朴な形はアナライザー形状のサブツリーで**200倍超**の壁時計を計測し、記録された却下形式だ）。WD2 = `Runner#run_analysis`ごとに鋳造され`scope.discovery`を通してシードされる実行世代トークン（ランナーのないプローブにはファイルごとの`def_nodes`アイデンティティのフォールバック;LSP / ADR-62のウォームループ再実行が累積するのではなくロールオーバーするよう単一の保持バケットスロット;エントリーはdef-nodeのオブジェクトアイデンティティでキーイングされ、id再利用に対して強く保持される —— 実行ごとにdefあたり最大2ノードアイデンティティ、プロジェクトインデックスのパース + 自ファイルのパース）を`DependencyRecorder.capture`/`replay`と対にする: 最初の計算の周りのobserve-and-forwardサブキャプチャが呼び出し先のフィルタされていない読み取り集合をエントリーに格納し（消費者ごとの自己読み取りフィルタはリプレイで再適用される）、ネストしたキャプチャは推移的なまま、ヒットはその集合を現在の消費者へリプレイする。WD3 = 候補性は「再帰ガードスタック上にない素のシグネチャ」に縮小する;ストアゲートは**要となる一時イベント深度ログ**（ガードヒット / unroll燃料の枯渇 / ADR-55 WD1クランプ / 不動点上限の崩壊、それぞれが参照したフレームのスタック位置でログされ、すべてがスペックで固定された監査テーブルを持つ`note_transient_fallback`のチョークポイントを通る）にわたって計算をブラケットし、イベントがブラケットのエントリー深度*より下*のフレームを参照したときだけ拒否する —— 祖先のコンテキストが結果に影響した;計算自身の機構（再帰メソッドのスタック最上部で収束した不動点）は問題なく格納するので、再帰の多いコードはメモを保つ。計測: mailの本体評価3,355→557（`has_content_type?` 564→2）、rigor lib 9,977→7,339、unroll-in-flight拒否は両方で→0;コーパスはバイト同一（mail / kramdown / mastodon app-models / redmine app-models / rigor lib）。WD4は2つの計測で死んだ設計を再追跡に対して固定する（finalization対応のサマリーテイントゲート —— `consult-tainted = 0`;param-read-maskキーの正規化 —— 別個のキーは≤ 9）;WD5はワーカーごとのforkスコープを保つ。却下: 記録の迂回（200倍超）;キーの粗視化 / eval-cap-then-widen（foldが変わる → バイト同一でない;精度と速度のトレードは[ADR-41](41-inference-budget-design/)に属する）;呼び出しノードごとの結果キャッシング（[ADR-52](52-compiled-plugin-contribution-dispatch/) WD5を再確認 —— このメモは入力でキーイングし、決してノードではキーイングしない）。根拠: [2026-07-13のコーパスパフォーマンスキャンペーンノート](../notes/20260713-corpus-perf-campaign/) + PR #79のカウンタテーブル |
| ADR-85 | [ファイルごとのシードバンドルと遅延def-nodeハンドル（事前パスのインクリメンタル化）](85-seed-bundles-and-lazy-def-node-handles/) | **Accepted — WD1（インクリメンタルなplugin-prepareキャッシュのスレッド化）を[PR #81](https://github.com/rigortype/rigor/pull/81)で着地;WD2/WD3（シードバンドル + 遅延ハンドル）を[PR #82](https://github.com/rigortype/rigor/pull/82)で**。[PR #77](https://github.com/rigortype/rigor/pull/77)のウォームの弧を完成させる: キャッシュHITは既に発見の事前パスをスキップするが、あらゆる`--incremental`リチェックはMISSパスの実行であり、1ファイルの部分集合を再解析する前にすべてのプロジェクトファイルをパース + ウォークしてクロスファイルインデックスを再構築する —— プラグインの少ないウォームインクリメンタル（rigor lib / mail）の80〜92%。**WD1**が最初（順序が要）: `IncrementalSession#build_runner`はすべての内部アナライザーを`cache_store: nil`で構築したので、プラグインの`cache_for`がディスクキャッシュを迂回し、各`#prepare`プロデューサーがクロスプロセスのリチェックごとに再計算した —— gitlabのウォームインクリメンタルの86%（1420万アロケーション）;セッションを通して`Cache::Store`をスレッド化する（`--no-cache`を尊重）とそれらのプロデューサーが提供でき、実行全体のADR-45結果キャッシュはこれらの実行から除外される（`run_result_cacheable?`が`record_dependencies` / `analyze_only`を落とす —— キャッシュ提供のベースラインは依存グラフを記録しない、部分集合実行の部分的な診断はフルキーを共有する）。計測: gitlab `app/models` **1670万 → 280万アロケーション（約6倍）**、`#prepare` **1430万 → 34万**。**WD2**は各ファイルの事前finalizeの寄与を、そのダイジェストでキーイングされたMarshalクリーンなバンドルとしてキャッシュし、`IncrementalSnapshot`にホストする（SCHEMA 5→6;6未満のブロブはnilとしてロード＝クリーンなコールドリビルド）;`ScopeIndexer.discovered_project_index_incremental`が正規のファイル順でバンドルを畳み込み（変更されたファイルだけを再ウォーク）、その後`finalize_def_index`を一度 —— マージされたテーブルはSet和 / 後勝ち / プロジェクト全体finalizeなので、これが健全な再構築だ（デルタパッチは不健全）。**WD3**はdef-nodeテーブルを*変更されていない*ファイルについてのみ`DefHandle(path, node_id, name, fingerprint)`として格納する（コールドベースライン + 再ウォークされた変更ファイルはLIVEノードを保つ —— 今ウォークしたばかりのファイルにオンデマンドの再パースはなく、混在テーブル）;3つのアクセサのチョークポイント（`Scope#user_def_for` / `#singleton_def_for` / `#top_level_def_for`）は`DefNodeResolver`——実行ごとに`(path, node_id)`あたり**1つの安定したノード**を生む実行ごとのパースメモ（ADR-84の戻り値メモはdef-nodeアイデンティティでキーイングする）——経由でハンドルを解決し、`node_id`が主 + `name`クロスチェック → 名前による新鮮ウォークのフォールバック → nil（保守的、決してサイレントでない）。ハンドルは`Runner#symbol_fingerprints`のために`fingerprint`を携える —— 偵察のQ6監査が見落とした4つ目のノード値の消費者 —— なのでADR-46の変更検出は再パースせずにそれを読む。基準（ADR-54、階層をADDするために適用）: load-and-foldは再計算を計測可能に上回らねばならず（36〜570倍の壁時計）、def-nodeの実体化は需要に従う（リチェックあたり0〜6ファイル）—— eagerなAST Marshalは却下（パースアロケーションの勝ちを失う）、ファイルごとの`Cache::Store`プロデューサーは却下（リビルドごとにN個のfs操作 対 スナップショット1回の読み取り）。計測されたプラグインの少ないウォームインクリメンタル: rigor lib **54万 → 14.9万**アロケーション（事前パス43.6万 → 2.5万）、mail **52.9万 → 5.4万**（48.5万 → 5千）;gitlabの結合スタック**1670万 → 206万（約8倍）**;コールド診断はmail / kramdown / mastodon app-models / redmine app-models / rigor libでバイト同一、`--verify-incremental`はグリーン。偵察からの乖離を記録（`classes`のキーリスト仮定は`Class.new(Super)`では偽;見落とされた`symbol_fingerprints`消費者）。根拠: [2026-07-14 P6シードキャッシュ偵察](../notes/20260714-p6-seed-cache-recon/) |
| ADR-86 | [残余のホットパスに対する部分的ネイティブ拡張（却下;rigor-rsがネイティブ速度を所有する）](86-partial-native-extensions/) | **Accepted、2026-07-14 —— 恒久的な却下**。v0.3.0キャンペーンの締めのプロファイルに対して評価した: 古典的にネイティブ化可能なホットパスは既にネイティブだ（rbsのCパーサ、prism、psych、digest/Marshal/zlib —— その残余コストはRuby側の実体化だ）、そして2つの純Rubyの残余はどちらも3部構成の基準に落ちる（候補は、要素ごとのRubyオブジェクトのFFIトラフィックのない表現完全なカーネルであり、YJIT/キャッシング/並列性で既に勝てるものではなく、ADR-27/29/31/15のプラットフォーム税に見合わねばならない）: フラットな推論エンジンには抽出可能なカーネルがない（それをポートすること＝より悪い経路でのrigor-rsだ）、そして等価性/ユニオンのカーネル（残余アロケーションの2〜5%、kramdown CPUの自己15.8%）は表現に縛られる —— ネイティブの勝ちには、計測された根拠で既に三重に却下されたインターニングの再設計が必要だ。分業を明示: ネイティブ速度の需要はrigor-rsの兄弟ポートへ誘導される（ADR-79の記録された分岐）;Rubyのrigorはそのエコシステム/相互運用/wasmの利点を保つ。1つの需要ゲート付きの非ネイティブ候補を最安優先で段階化（WD4）: モノレポのglob+ダイジェストのウォームフロア —— Rubyスレッドプール → FSイベント/デーモン → その後にはじめてネイティブのバッチハッシャー。Goは特に却下（cgoのインプロセス適合）。再評価トリガー: 基準1を満たすコーパス全体の純Rubyフレームが≥15%、等価性バケットが約15%を超えて成長、rigor-rsの放棄、またはWD4のはしごが非ネイティブの段を使い果たすこと。 |
| ADR-87 | [ヌルビルドフロア: stat-then-digest検証、ゼロ変更のスナップショットスキップ、ヒットパスのブートスリム化](87-null-build-floor/) | **Accepted — WD1〜WD5を実装（[PR #85](https://github.com/rigortype/rigor/pull/85)）**。ウォームパスをビルドシステムの用語で捉え直し（NULLビルド＝変更ファイルゼロ、および単一編集ビルド）、本質的でない3つの計測されたフロア構成要素を除去する。**WD1** —— `digest + (size, mtime_ns, ctime_ns, inode) + 記録瞬間`をまとめる`:stat` `FileEntry`比較器: 検証はまずstatし、タプルが動いたかレース性ガードが発火したときだけ再ハッシュするので、真のnullは**コンテンツバイトを0**ハッシュし、その一方でSHA-256ダイジェストが唯一の変更権威であり続ける（gitインデックスモデル;`ctime` + inode + レース窓;脱出ハッチ`cache.validation: digest` / `RIGOR_STRICT_VALIDATION=1`）。`:stat`は検証専用の記述子に乗る;KEY記述子は`:digest`を保つ。`SCHEMA_VERSION` 4→5。**WD2** —— `GlobEntry`の集約シグネチャが、コンテンツダイジェストではなくファイルごとのSTATタプルにわたるSHA-256になる（変更されていない監視ツリーではコンテンツバイト0）;ドラフトのファイルごとテーブルは**却下**（2万ファイルのテーブルがgitlabインクリメンタルをMarshalで1.85s→5.3sにリグレッションさせた —— 監視される依存は全か無かなので、部分的な再ハッシュは重いテーブル以外に何も買わなかった）。**WD3** —— `run_incremental`が、ゼロ変更のリチェックで無条件の`snapshot.save`をスキップする。**WD4** —— 軽量な`RunCacheProbe`が、エンジンがロードされる前に通常の`check`のHITを提供する: 共有の`RunCacheKey`（設定由来のライブラリ + `RBS::VERSION`、envビルドなし）を通じてADR-45キーを構築し、それを`Store#peek_validated`するので、HITの`$LOADED_FEATURES`には**`rigor/inference`がない**（サブプロセススペック + MISSコントロールで固定）;ルールID定数を軽量な`check_rules/rule_ids.rb`へ切り離し、`coverage_scan`/`check_runner_factory`を遅延requireすることを要した。**WD5** —— 陳腐化バッテリー + touchはHITのままのエンドツーエンドガード。計測（ホスト）: gitlab null 1.41s→**0.34s**（115 MB→0ハッシュ）、gitlab null-incremental 1.85s→**1.44s**、小規模プロジェクトnull約0.36s→**約0.22s**;gitlabの1編集は正直に**中立**（クロージャバウンドであってglobバウンドではない —— 偵察のglobハッシュ帰属は訂正された）。[ADR-54](54-cache-slimming/)の却下されたmtime高速パスを優越する（ダイジェスト権威を保つ）。根拠: [2026-07-14ヌルビルド偵察](../notes/20260714-nullbuild-recon/) + [2026-07-13コーパスパフォーマンスキャンペーンノート](../notes/20260713-corpus-perf-campaign/) |
| ADR-88 | [インクリメンタルなプラグインファクトの健全性](88-incremental-plugin-fact-soundness/) | **Accepted — WD1〜WD4を実装（[PR #89](https://github.com/rigortype/rigor/pull/89)）;WD5は見送り**。`--incremental`スナップショットのグローバルフィンガープリント（設定 / gem / RBS env / `signature_paths:`）は、それらの入力が取りこぼすファイルから計算されるプラグインの値を捕捉しない —— `rbi_paths:`下の`.rbi`ツリーからのSorbetカタログ、`db/schema.rb`からのARの`model_index`など。そうしたファイルを編集すると、変更されていない呼び出しサイトが解決する型が、解析対象のどのファイルにも触れずに動くので、リチェックは陳腐化したキャッシュ済み診断を提供してしまう（プラグインの読み取りは内部的で`Scope`のチョークポイントではないため、ADR-46のエッジがない）。**WD1** —— `Analysis::PluginFactFingerprint`がプラグインの**ファクトサーフェス**（（a）ADR-9の`fact_store`のパブリケーション、（b）ADR-60のプロデューサーのVALUE、（c）任意の`incremental_state_fingerprint`フック）をダイジェストし、スナップショットに乗り（`SCHEMA` 8→9、9未満はnilをロード）、不一致で無効化する（バナー + `--cache-stats`に表面化）。プロデューサーの**値**をダイジェストし、そのキャッシュブロブはダイジェストしない（ブロブは入力記述子を携える → 値を保存する編集で過剰に無効化する: 計測されたgitlabのコントローラー編集 → フル23s対 インクリメンタル約9.5s）。そして解析ランナーの準備済みレジストリから事後的に読まれる（リチェックのprepare + メモ化されたプロデューサーを再利用 → 約0.24s ≈ 2.5%、プール化モードのフォールバックとしてのみ保たれる約1.0sの別プローブに対して;両パスは一致するので、決定はプール非依存）。サーフェスを持たない寄与プラグインは**不透明**だ —— 名指しされ、実行ごとにスナップショットが再利用不能になる（強制関数）;同梱の寄与セットはすべて非不透明だ（プロデューサー、または`--verify-incremental`で裏打ちされたファイルごと/静的なフックのセンチネル）。**WD2** —— SorbetカタログがADR-60の`producer :catalog`になる（シグネチャツリーを`watch:`し、Marshalクリーンな`{catalog, sigil_by_path, parse_errors}`バンドル;`Dir.glob`はデフォルトでソートするので、last-sig-winsの畳み込みは決定論的）、ディスクにキャッシュされ + WD1のダイジェストを無料で受ける。**WD3** —— `Scope#user_def_site_for`が、その兄弟`#user_def_for`が記録するクロスファイルのメソッドエッジを記録するので、def-siteの行シフトが`call.undefined-method`の消費者を再チェックする。**WD4a** —— 捏造した編集バッテリー（Sorbet `.rbi`のシグネチャ編集が消費者を無効化 + 再解析、赤→緑;dry-typesのエイリアス + ARスキーマ列のダイジェストが動く;WD3の行シフト;不透明性;プール化/逐次のパリティ）。**WD4b** —— 既存のgitlab `--verify-incremental`の赤（1,161件がインクリメンタルのみ、すべて`:info`のAction Packトレース）は、プロデューサーではなく**プラグインローダーの冪等性バグ**に根本原因があった: verifyハーネスはbaseline→subset→fullを1プロセスで実行し、`full`のオラクルは2度目のインプロセス`Plugin::Loader.load`で、その`require`がノーオプ → 新規登録デルタが空 → 素の文字列エントリーが「どのプラグインも登録しなかった」に当たる → オラクルはプラグインをゼロロードした（101対2,494）。ローダーで修正（`Plugin.record_gem_registration` / `ids_for_gem`が再ロード時にgemのidを回復する）;修正後、gitlab verifyはバイト同一だ（887/1,774、2,494、**不一致0**）。**WD5**（消費者ごとのプラグイン読み取り追跡）は見送り —— スナップショット全体の無効化は健全であり、値ダイジェストの精度 + プロデューサーの決定論をもってすれば、よくある値保存の編集で過剰に無効化しない。精度加算的（型/診断/重大度の変更なし;コールド診断はバイト同一）。根拠: [2026-07-14インクリメンタルプラグインファクト監査](../notes/20260714-incremental-plugin-fact-audit/) |
| ADR-89 | [セマンティックな伝播ゲート: 宣言シェイプと観測キーの戻り値サマリー](89-semantic-propagation-gates/) | **Accepted — WD1（宣言シェイプゲート） + WD2（観測キーの戻り値サマリーゲート）を実装（[PR #90](https://github.com/rigortype/rigor/pull/90)）**。PR #88のB1コメントのみゲートをBODY編集へ拡張する: 依存は、それが消費できる何かが実際に変わったときにのみ再解析される（ADR-88のプラグインファクト値フィンガープリントの上でのみ健全）。#87/#88の後も2つの過剰伝播が残る: **(a)**どんな本体編集も、すべての祖先/ファイルレベルの依存を再解析する（偵察のS5a —— 13呼び出し元の`def self.safe_find_or_create_by`の本体編集 → 341ファイルだが、それらはクラスの*宣言シェイプ*だけを消費し、本体編集では変わらない）;**(b)**戻り値がすべての観測された呼び出しシェイプで変わらない本体編集でも、依然としてすべての呼び出し元を再解析する。基準: 編集されたFの依存Dは、（DがFから消費できるもの）×（変わったもの）が非空のときにのみ再解析される。これは、それぞれが**完全な**消費可能サーフェスをカバーする**永続化された**サマリーを比較することで証明される。**WD1** —— `ScopeIndexer.declaration_signature`: ADR-85のシードバンドルの、defごとのCODEフィンガープリントを、defごとのSIGNATUREシェイプ（名前、種別、完全なパラメータ構造、可視性、祖先、メンバーレイアウト、メソッドの存在、AND defの開始行）に置き換えたもの。単一ファイルのライブインデックスから計算される（パラメータ構造はdefノードから直接 —— 意図的に構文的）。宣言が安定した変更ファイルは`unstable`から外れるので、その祖先/ファイルレベルの依存はスキップされる;これはB1を一般化し（コード安定 ⟹ 宣言安定）、ゲートはコメント除去済みのコードフィンガープリントをオフにした（`SCHEMA` 9→10）。defの行はシグネチャの中に生きる（**ドラフトのサイトごとの精度からの乖離** —— ADR-88 WD3のシンボルフィンガープリントは行不変なので、ファイルレベルのエッジだけがADR-17の`project_definition_site`消費者をカバーする）: 行をシフトする本体編集は保守的に宣言不安定である（完全な依存集合を保つ、＝B1の振る舞い）、SAME-LINEの本体編集だけが縮む。**WD2** —— defごとの観測キーの戻り値サマリー（ADR-84メモの`(receiver, args) → return`エントリー、defあたり8キー · 4000 defで制限、`describe(:short)`記述子 + ADR-56のコンテンツ変異エフェクト集合）;`MemoEntry`は呼び出し記述子を得たので、サマリーが実際のキー型を携え、`ExpressionTyper.harvest_return_memo` + `Runner#return_summaries`経由で収穫される。リチェック時、宣言が安定しシグネチャが変わらない変更defは、セッション側のプローブ（`Runner#evaluate_return_types` —— 発見 + envを一度構築し、ファイル解析なし、安定ペアがサマリーを携えるときにのみ実行されるので、コメント編集はWD2コストをゼロ支払う）を通して、各古いキーで再評価される（`Scope#user_method_return` → メモ、最終値のみ）;すべての戻り値 + エフェクト集合が等しい → そのdefのシンボル依存を落とす、そうでなければ保つ（保守的）。戻り値のドロップは`gate_eligible_def?`でゲートされる（**乖離 —— 健全性の完全性**: 呼び出し元は呼び出し先のivar確定代入 + `yield`値も消費するので、ドロップはivar/cvarを書かず、yieldせず、暗黙selfの呼び出しをしないdefでのみ発火する —— 戻り値 + コンテンツ変異がクロスファイル本体サーフェスの完全な集合であるような保守的十分条件;一般の全サーフェスゲートは見送り）。**WD3** —— ゲートはADR-88のプラグインファクトフィンガープリントが一致したときにのみ適用され、仮定ではなく表明される（ゲート付きリチェックは`run_incremental`の`if reuse`分岐の内側でのみ信頼される;不一致/不透明なプラグインはそれを破棄する）、加えて保持されたコメント取り込みのオフスイッチ。**WD4** —— 捏造したバッテリー（アリティ/可視性/追加メソッド/戻り値可視 → 伝播;同一行/戻り値保存 → スキップ;変異エフェクトの変更/不適格def → 保つ;行シフト → サイト消費者が再チェック）、それぞれフルとバイト同一。計測: gitlab `safe_find_or_create_by`の戻り値保存編集**341 → 1**（WD1は340の祖先依存をすべて落とす;in-scopeのシンボル呼び出し元は0）、`label.rb`の`reference_prefix`リテラル変更**19 → 1**、両方バイト同一;`--verify-incremental`はバイト同一（887/1,774、2,494、不一致0）;コールドmail 26 / kramdown 68はバイト同一;S1リチェックの壁時計はmasterのノイズ内。計測の乖離: gitlab S5aのクロージャは偵察が見積もった約13〜14ではなく**1**だ（`safe_find_or_create_by`は記録されたin-scopeシンボル呼び出し元を0持つ）、なのでWD1がgitlabの目玉で、WD2の戻り値ドロップは捏造したバッテリーで証明される。根拠: [`20260714-edit-shape-recon.md`](../notes/20260714-edit-shape-recon/) |

## 新しいADRの追加

重要なアーキテクチャ上の決定を行うとき：

1. このディレクトリで次に空いている番号を見つけます。
2. 既存のADRからテンプレートをコピーするか、同じ構造（Status、Context、Decisions、Consequences）に従って新しいファイルを作成します。
3. 上記の索引テーブルにエントリーを追加します。
4. 適切なコードコメント、他のADR、または`AGENTS.md`からそのADRを参照します。

## 他のドキュメントとの関係

- **`docs/types.md`** — 型仕様のクイックガイド。ADR-1と`docs/types.md`が同じ領域を論じているとき、*アナライザーが何をするか*については`docs/types.md`が権威を持ち、*なぜそうするか*についてはADR-1が権威を持ちます。
- **`docs/type-specification/`** — 規範的な型仕様。トピックごとのドキュメントに分割されています。
- **`docs/internal-spec/`** — アナライザー内部の契約（contract）（エンジンサーフェス（surface）、型オブジェクトの公開API）。
- **`docs/handbook/`** — エンドユーザー向けハンドブック。静的型付けの予備知識を持たないRubyプログラマー向けに書かれています。
- **`AGENTS.md`** — このリポジトリで作業するエージェント向けの開発契約。
