{
  "name": "rigor-docs-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "sync:docs": "node scripts/sync-rigor-docs.mjs",
    "check:translations": "node scripts/check-translations.mjs",
    "bootstrap:translations": "node scripts/check-translations.mjs --bootstrap",
    "predev": "pnpm run sync:docs",
    "dev": "astro dev",
    "prebuild": "pnpm run sync:docs",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "5.0.4",
    "@astrojs/starlight": "0.39.1",
    "astro": "6.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "6.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
